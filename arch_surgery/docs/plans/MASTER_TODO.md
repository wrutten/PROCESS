# Master TO-DO — architecture surgery on PROCESS

| | |
|---|---|
| **Status** | ACTIVE — the standing execution queue for this repository |
| **Owner** | W.J. Rutten (paces execution); orchestrating agent dispatches |
| **Objective** | Determine whether **the arrangement of solvers and optimisers alone** — every physics and engineering model frozen at `c0ae5b28` — measurably changes the cost of solving PROCESS, by partitioning the global idempotence loop into per-module solvers |
| **Base commit** | `c0ae5b28` — frozen (D2). Shared coordinate system with `functional_PROCESS` and with the dependency-analysis pin `PROCESS_at_36ac820e` |
| **Supersedes** | The IDF experiment at `710a75c9` (`IDF_EXPERIMENT_PLAN.md`, `idf_probe/MEMO.md`, `NOISE_ANALYSIS.md`). Retained for methodology only; **no number from it is evidence** (D4) |
| **Stage detail** | [`../MDA_PARTITION_EXPERIMENT.md`](../MDA_PARTITION_EXPERIMENT.md) is the experiment plan — hypothesis, evidence, critical assessment, stages and gates. This file is the queue that sequences them |

---

## Project administration

**Orchestration protocol** (in force):

1. **The orchestrator dispatches; task agents plan and execute.** The orchestrator does not
   execute tasks itself.
2. **Every task is labeled `A<n>` with a keyword minted at the same time**, and the keyword
   always accompanies the number in prose — queue rows, change-log entries, reports and
   session messages write `A1 (stage0-rebaseline)`, never a bare `A1`. One keyword per task,
   never renamed. The keyword is the branch slug.
3. **Every task runs on its own branch** `A<n>-<keyword>` **in an isolated `git worktree`** off
   `architecture_surgery`, **created by the orchestrator, never by the agent harness** (I-11).
   Run [`../../bin/new_task_worktree.sh`](../../bin/new_task_worktree.sh) `A<n> <keyword>` first;
   it branches at the `architecture_surgery` tip, asserts the tree the agent will actually get —
   named tip, descent from `c0ae5b28`, *not* upstream `main`, and `arch_surgery/`, `CLAUDE.md`
   and `TRAPS.md` all present — tears itself down if any assertion fails, and prints the path.
   Put that path in the brief and dispatch **without** harness worktree isolation. Worktrees live
   in `/home/wrutten/projects/PROCESS_surgery_worktrees/<branch>`, outside the repository; that
   directory is **ours**, and is not one of the sibling clones the hard rules forbid writing to.
   The agent writes
   a report to [`../reports/`](../reports/) — verdict first, autonomous decisions with their
   reversal paths, append-only change log. Commit messages carry the keyword:
   `A<n> <keyword>: …`.
4. **Reports avoid jargon.** Project shorthand — decision numbers, issue numbers, internal
   vocabulary — is spelled out at first use, so a report reads without the queue open beside it.
5. **The orchestrator appends a critical assessment to every task report** and acts as
   adversarial reviewer. **The assessment happens *before* the merge, and gates it** (amended
   2026-09-01, user). If the review turns up work — a gap, an unstated assumption, a missing
   artifact — it goes back to the task agent on its own branch, and the branch is merged once,
   afterwards. Reviewing after merging turns one clean unit of history into two and removes the
   reviewer's only lever. **This rule was written because the orchestrator merged A18
   (experiment-framework) and then found a reproducibility gap in it**; that fix now lands as a
   second merge, which is exactly the outcome the rule exists to prevent.
6. **A failed gate blocks the merge and is reported as a result.** No tuning a gate into
   passing, no conditional merges that defer a fix to a later task. The fix is made on the same
   branch and the task is re-gated as one merge.
7. **Merge discipline**: merges happen from the main checkout, one at a time; the branch is
   deleted with `-d`, never `-D`. **Retire the worktree with**
   [`../../bin/retire_task_worktree.sh`](../../bin/retire_task_worktree.sh), never a bare
   `git worktree remove` — run artifacts are untracked by design, so removal destroys them unless they are
   relocated first (I-14, which cost the evidence behind a headline correction). At merge the task report is archived to
   [`../reports/deprecated/`](../reports/deprecated/) — the live directory holds only open
   tasks' reports.
8. **Pacing**: the user decides which tasks execute. Only the user adds tasks; agents may
   propose.
9. **Stop background work with `TaskStop`, never `pkill`.** Each sandboxed Bash call has its own
   PID namespace, so `ps` sees nothing from a sibling call and `pkill` kills nothing while
   reporting success (trap T8; A2 lost a set of runs to this).
10. **One actor per working tree.** Never `git commit` or `git checkout` in a tree another agent
   is working in — commits land on whatever branch that agent last checked out (I-6, 2026-08-31).
   The orchestrator's admin work goes on `architecture_surgery` in the main checkout; task work
   goes in its own worktree.
11. **DSM findings go in a standing register, not a task report.** Anything found to be wrong, missing or misleading about the dependency analysis — a phantom edge, a dead edge, an extraction artefact, a granularity mismatch — gets an entry in [`../reports/DSM_VALIDATION.md`](../reports/DSM_VALIDATION.md) **when it is found**, with the edge, the evidence, which artifact is wrong and the consequence. That report **accumulates and is never archived** at merge, unlike task reports (protocol §7). Rationale: trap T1 has now been rediscovered three times independently, once inside a plan's central section, because each finding was buried in the task report that happened to find it. The register is also a deliverable back to `PROCESS_code_analysis`.
12a. **Never `git add -A` unscoped in this repository.** Stage explicit paths —
   `git add arch_surgery process <file>` — or `git add -u`. The agent sandbox bind-mounts
   `/dev/null` over `.bashrc`, `.mcp.json`, `.claude/` and about eighteen other paths, so they
   appear in the working tree as **character devices** that `git status` reports as ordinary
   untracked files. A bare `git add -A` stages them silently; **the orchestrator did exactly that
   in `83e18d15` and committed 21 of them**, and only noticed because the *next* `git add -A`
   aborted with `can only add regular files, symbolic links or git-directories`. They are now in
   `.gitignore`, which stops a recurrence but would not have stopped the first one — the habit is
   the fix.
12. **A gate must be shown capable of failing before its zeros are accepted**, and every count
   of mismatches carries the **denominator** of things actually compared. Perturb the thing the
   gate watches by the smallest amount that should register — a 1-ULP change to one output is
   enough — and confirm the gate catches it. Rationale: this project has twice published a `0`
   over a population quietly smaller than the one named (trap **T11**), and has once had a gate
   predicate that returned PASS on an empty set. **A3 (build-reorder) adopted this unprompted and
   it paid immediately** — the sensitivity check exposed a defect in the agent's own comparator,
   an MFILE line parser anchoring on the first `(...)` that silently dropped ~1 000 floats per
   scenario. A passing gate whose failure mode has never been exercised is an assertion, not a
   measurement.
13. **The branch point is asserted by the tool that creates the worktree, not by the agent.**
   §3's script does it. Briefs should still *name* the tip they expect, because a brief is read by
   a human too, but no task should depend on an agent noticing that its own tree is wrong.
   Rationale, measured rather than assumed: the harness seeds worktrees from the repository's
   **default branch**, and **7 of 7** worktrees this project created landed on `main`. `main`
   carries no `CLAUDE.md`, no `arch_surgery/` and no `TRAPS.md`, so a mis-seeded agent starts with
   **none** of these rules loaded — the guard cannot live in the tree it guards, which is why the
   fix had to move to the dispatch side. **Note also that "descends from `c0ae5b28`" is worthless
   as a check on its own**: upstream `main` descends from it too, so that test passes on exactly
   the tree it is meant to catch.
15. **Every published number is produced by executing a committed Python script, never by an
   ad-hoc command line** (user, 2026-09-02: *"Experiment results should always be created by
   execution of a reproducible python script. I don't want future experiments to be run manually
   in command line. I want committed traceability of the results."*). Binding on every task from
   A28 onward. Concretely:
   - **The script is committed before the numbers are published**, in the same branch, and the
     report names it and the commit it was run at. A report that cites a figure no committed
     script can regenerate is incomplete, not merely undocumented.
   - **No stage may exist only as a shell invocation.** A run reachable only by retyping a
     `python -m ...` line with flags is not reproducible; wrap it as a stage of the experiment
     script. Shell is permitted for *inspection* — `ls`, `grep`, reading a JSON — never for
     producing a number that a report will cite.
   - **The script owns the whole chain**: deck derivation, gates, calibration, campaign, ladder,
     analysis, tables. If a stage was run by hand during development, it must be folded into the
     script and the script re-run before the task reports.
   - **The report states, per figure, which stage produced it**, so a reader can go from a number
     to the code that made it without reading the queue.
   - **Failure is reproducible too.** A gate that failed, a deck that was dropped, a start that
     was refused — all reachable from the same entry point, so the negative results are as
     traceable as the positive ones.
   Rationale: this project's published headline has already been overturned once (A26) and had
   three separate errors corrected in a merged report (A21). Both were caught only because the
   artifacts happened to survive; **I-14 shows they do not always survive** — A26's
   `matched_accuracy.json` was destroyed and Phase A's figures now have to be regenerated rather
   than re-read. A committed script makes the numbers recoverable from code instead of from
   artifacts that a `git worktree remove` can delete.
14. **Standing rules**: the sandbox is never overridden (report blockers and ask); never push
   without per-push approval; never modify a sibling clone; the base commit and the models
   are frozen. See [`../../../CLAUDE.md`](../../../CLAUDE.md).
16. **Every table carries a concise caption** (user, 2026-09-04): units, and the semantics of
   the entries — what a row is, what a column is, what population the numbers summarise, and
   which construction produced them (e.g. "converged pairs only", "identical-ok-set sums").
   A table whose meaning requires reading the surrounding prose to decode is incomplete.
   Binding on every report from the V2 experiment report onward.

**Work-item terminology** (one word, one meaning):

| Term | Label | Meaning | Rules |
|---|---|---|---|
| **Task** | `A<n>` | A queue row: one branch, one agent, one report, one merge decision | Only the user adds tasks |
| **Subtask** | `A<n>.<k>` | The agent's own decomposition, in its report | Never its own branch or report |
| **Issue** | `I-<n>` | Defect or gap in *this repository or its environment*, found outside a task's scope | Filed below, not fixed in passing |
| **PROCESS finding** | — | Defect or critique of *PROCESS itself* | Architecture critiques belong here; implementation defects go to `PROCESS_code_analysis/docs/bug_reports/` |
| **Decision** | `D<n>` | A recorded user decision | Append-only; a reversal is a new decision referencing the old |

**Numbering** — next free: **A43**, **D20**, **I-16**. Numbers are never reused. *(Last corrected 2026-09-03; it had drifted twice before — A20/D13/I-11 while the queue reached A30/D18/I-14, then A31 while A31–A33 were minted — check it when you add a row.)*

**Optimiser-registry allocation** is administered in
[`REGISTRY_ALLOCATIONS.md`](REGISTRY_ALLOCATIONS.md) — one table, append-only (D10). Every task
takes its numbers from there; two branches allocating independently would both pick the same next
number and merge into a dict with one entry silently winning.

---|---|---|
| Constraints | 82 registered over 1–92, cap 500 | **93** — but `lablcc` has exactly 92 entries and must be extended in step |
| Iteration variables | 83 registered over 1–177, `N_ITERATION_VARIABLES_MAX` (**178 as of A24**; the constant is derived, `max(keys)`) | **none** — 177 is taken and the cap is the maximum |

**No iteration-variable block can be reserved until the cap is raised (I-7).** Constraint numbers
from 93 upward are available and will be blocked out when the first lifting task is authorised.

---

## Decisions (live set)

| # | Decision | Date |
|---|---|---|
| **D1** | `wrutten/PROCESS` is the canonical fork. `PROCESS_surgery` is flattened into it — the research artifacts live in `arch_surgery/` inside the fork, not in a separate repo with a submodule. `IPP-SRS` branches (`AST_parseable`, `stage0-probe`) are archived on the fork for reachability but are not merged | 2026-08-31 |
| **D2** | **The base commit is `c0ae5b28`, frozen.** Chosen because it is the last commit before `functional_PROCESS` begins its rewrite and is the ancestor of the dependency-analysis pin — the three studies share one coordinate system. Not to be rebased or re-pinned; `upstream` may be fetched to measure drift only | 2026-08-31 |
| **D3** | **A fresh rewrite.** The `PROCESS_rewritten/` scaffolding on `stage0-probe` (based on `710a75c9`) is not ported | 2026-08-31 |
| **D4** | **The `710a75c9` evidence is discarded**, raw run artifacts deleted. The superseded documents are retained for methodology only; every number is rederived at `c0ae5b28`. Recoverable from `adf863d7` if a specific figure is ever wanted | 2026-08-31 |
| **D5** | **The models are frozen; only the driver changes.** This is what distinguishes this study from `functional_PROCESS`: a rewritten back-end cannot isolate the architecture's contribution, because any measured difference confounds the architecture with the rewrite | 2026-08-31 |
| **D6** | **Correctness is gated on `norm_objf` plus a post-solve feasibility audit, never on iteration variables.** Some iteration variables are not identified by the problem and differ at an unchanged optimum, so an itvar gate generates false alarms | 2026-08-31 |
| **D7** | **A full IDF / MDF / SAND comparison is deferred** to a later study on the `functional_PROCESS` back-end. This experiment is not a stepping stone to it — it is the control that study will need | 2026-08-31 |
| **D8** | **The module partition is derived from the collapsed DSM, not assumed.** M1 Physics = rows 4, 6–28; M2 Coils = rows 5, 29–37; M3 Plant = rows 40–51; `CsFatigue` (38) and rows 52–55 feed-forward; `Pulse` (39) is the articulation point belonging to no module | 2026-08-31 |
| **D9** | **The archived scenario deck is patched in place, not re-pointed at upstream's regression inputs.** `st_regression.IN.DAT` gains `i_tf_turn_type = 2` and the four tape geometries; the other three stay as archived and continue to load via obsolete-name rewriting (A1 autonomous decision 3). Rationale: the deck stays a frozen artifact of this study rather than tracking whatever upstream ships, so a scenario cannot change under a result | 2026-08-31 |
| **D10** | **Registry numbers are appended, never fitted into gaps.** `ITERATION_VARIABLES` has 94 gaps in 1–177 from retired variables; reusing one silently reinterprets any existing `IN.DAT` naming that number. There is **no cap to raise** — `N_ITERATION_VARIABLES_MAX` is derived as `max(keys)`, so appending 178 raises it automatically and every array sized by it grows. Constraints append from 93, with `lablcc` extended in step. Allocations in [`REGISTRY_ALLOCATIONS.md`](REGISTRY_ALLOCATIONS.md) | 2026-08-31 |
| **D11** | **D5 refined: minimal edits to `process/models/` are permitted, and every change requires the user's approval before merging.** The physics is still frozen — what is licensed is structural work such as extracting a residual so its solution method becomes a driver choice, not changing what a model computes. The approval gate replaces the blanket prohibition. **Unblocks A9–A11** | 2026-08-31 |
| **D12** | **The MDA partition experiment proceeds despite the gate**, under the plan's own 10–25 % rule ("proceed with the expectation revised down and stated" — now written out in its §4). **The feed-forward hoist, formerly candidate E1 / task A13, is folded in as the partition's Stage 1b** rather than run as a separate experiment: it is the only separable component and is taken first so a positive result is banked before the risky work. A4 and A5 are reinstated | 2026-08-31 |
| **D13** | **The partition is measured in two phases: fixed-point first, VMCON second.** *Phase A* removes the optimiser entirely and compares **flat Gauss-Seidel (control) against block Gauss-Seidel** (M1/M2/M3 inner solves inside an outer loop over the `k = 1` coupler), replayed over design points harvested from a baseline run. Both arms converge on **all coupling variables** to tolerance, not on `objf`/`conf` — so **VP3 (A14) is un-deferred as a Phase A prerequisite and the control is re-baselined under the same predicate**. Consequences: H5 is absent from Phase A rather than mitigated; the primary metric becomes a **count**, not wall clock, which resolves the standing conflict between the plan's §6 and the no-timings rule; and Phase A can test VP4 **without** VP5, since an outer loop hosts the coupler where a lifted constraint would have needed VMCON — **inverting the Stage 3 → Stage 4 dependency**. *Phase A parameters:* inner tolerance **tight** (accepted as conservative against the partition — the inexact-block regime is a later axis if needed); hard cut **20**, reaching it ⇒ **invalid result**, never a budget. **A2's and A19's `Sᵢ` are predicate-bound and do not transfer to Phase A.** *Phase B* then asks the sharper question — does hosting the coupler on VMCON as a design variable beat hosting it on an outer fixed-point loop? — with Phase A as its control | 2026-09-01 |
| **D14** | **Phase B implementation decisions** (user, 2026-09-01). **(a)** The `lablcc` extension in `process/data_structure/numerics.py` is **approved** — it sits outside `CLAUDE.md`'s default-permitted surface (`caller.py`, `process/core/solver/`, the probe) but D10 mandates it for any constraint append, and A4 cannot append constraint 93 without it. Unlike A20's withdrawn placeholder, this one is genuinely unavoidable. **(b)** **D11 approval granted** for the residual extraction in `process/models/pulse.py` — structural only: the expression is unchanged and its *solution method* becomes a driver choice. **(c)** **Phase B's baseline is PROCESS as it currently is** — existing predicate, existing flat loop — not Phase A's reimplemented flat arm. This removes a confound: our harness against PROCESS's loop compares two codebases, while baseline-PROCESS against modified-PROCESS varies one thing. **Consequence — REVISED by the user, 2026-09-01, overriding the orchestrator's call.** The variant's per-module solves use **Phase A's coupling-state predicate, fully converged**, with τ set by a ladder as in Phase A. The orchestrator had proposed reusing PROCESS's `check_agreement` per module, to keep architecture the only variable. That framing was wrong: **a per-module solver cannot use a global objective/constraint test, because one module does not determine those quantities.** The predicate change is *entailed by* the architecture change, not an independent choice alongside it, so it is not a confound. Phase A measured the predicate's isolated effect at −3.4 % to +8.6 % if the two ever need separating. **The baseline still reproduces `check_agreement` and its defects in full** — that half is unchanged. **The baseline reproduces `check_agreement`'s known defects in full** (the `equal_nan` NaN loophole and the hidden `atol = 1e-8`) — deliberately, because the baseline is *PROCESS as shipped* and silently repairing it would flatter the comparison. The defects are filed separately and are not thereby endorsed | 2026-09-01 |
| **D15** | **Phase B experiment design settled** (user, 2026-09-01), on top of the H5 protocol in [`MDA_PARTITION_EXPERIMENT.md`](MDA_PARTITION_EXPERIMENT.md) §2.5. **(a) Perturbation size is calibrated, not chosen**: try δ ∈ {1 %, 5 %, 10 %} on the baseline alone and take the largest that keeps `ifail = 1` on most starts. **(b) The feed-forward hoist is INSIDE the Phase B variant** — it is an architecture change too and counts toward the existence proof; an excluding arm follows later (the framework already has it as a toggle, VP2). **Consequence to state in every report: the Phase B headline is the combined effect of lift + module solvers + hoist, and must be called "the proposed architecture", never "the partition's benefit"** — the hoist is separable and quoting a combined number as the partition's would repeat the units error of trap T11. **(c)** A start whose `norm_objf` differs beyond tolerance is **excluded from the cost comparison and counted as a robustness finding**, reported per arm, never silently dropped. **(d)** A per-module solve that fails to converge **raises and counts as a failed start**, matching the baseline's behaviour so the two arms' failure modes are comparable. **Orchestrator's calls, approved:** the burn time is perturbed with the same δ as every other design variable (otherwise the arms have different perturbation dimensionality); `st_regression` gets the module-solver treatment **without** the lift and is reported as the `k = 0` control; τ starts at Phase A's 1e-6 with **multi-start success rate** as the confirmatory signal rather than objective movement, since with the optimiser present what τ protects is gradient quality | 2026-09-01 |
| **D16** | **Phase B is implemented in two bundles, not four tasks** (orchestrator, 2026-09-01, on the user's instruction to bundle more before re-running tests). **A24** = F2 + F9 + F6; **A25** = A4 + A5 + gate + H5. The rationale is not merely speed. **A24's three pieces are all inert**, so they share *one* bit-identity gate and a failure is still trivially attributable — nothing in the bundle should change anything, so any difference localises itself. **A25's two pieces were never separable**: A19 measured `max Sᵢ` unchanged when the coupler is pinned, so A4 alone buys nothing and has to be measured with A5; bundling them follows that finding instead of fighting it. Cost: **4 gate cycles become 2**, each cycle being four decks of isolated PROCESS runs. **Risk accepted and stated:** if a bundle's gate fails, attribution costs more than it would have for a single task — mitigated in A24 by the inertness premise (which the agent is told to flag loudly if it does not hold for some piece) and in A25 by the fact that its components were always going to be one measurement. **The user granted autonomous go-ahead to implement and run Phase B** the same day, so A25 proceeds to H5 on a passing gate without a further check-in — but **stops and reports on a failing one**, and does not run H5 in that case. |
| **D17** | **Methodology is fixed before Phase B is re-run, and `large_tokamak_eval` is dropped** (user, 2026-09-01). The user ruled on the results report's §6 point by point. **Two calls were put back to them and both were decided against the cheaper option.** §6.3(ii): rather than keep excluding components that never varied and rely on a runtime assertion, **every component is tested** and near-zero scales get a recorded floor — accepting that some design points may become invalid, which is reportable rather than a problem to tune away. §6.5.3: `large_tokamak_eval` is **dropped**, not enlarged — the orchestrator was asked for reasons to keep it and found none that bear on an *optimisation* performance study, since the deck runs **0 solver iterations**, never enforces its inequality constraints, and A22 had already found its evidence weaker than the other pulsed decks. It was carrying two of the report's largest percentages on the least foundation. **Timings return, but only as context** (user: "give an uncertainty band"), which is consistent with the standing rule rather than an exception to it — I-10 measured 35 % CPU-time spread on identical work, so every timing carries a median, an interval, a repetition count and its position in the run sequence, and acceptance stays on counts and bit-comparisons. **Already-merged reports are not retro-edited**: their four-deck tables stand as the record of what was run, and the drop is dated so a later three-deck table is not misread as one with a missing column. |
| **D18** | **Phase B gains a third arm: a predicate-matched control** (user, 2026-09-02). **D14(c) is amended, not overturned.** Its first half stands — the baseline stays **PROCESS as shipped**, because that is what makes the result relevant to a real user. Its second half was wrong: it claimed this made the comparison single-variable, and it does not. The baseline and the variant stop on **different predicates** — PROCESS's `check_agreement` tests the objective and constraint vector with `np.allclose`'s hidden `atol` and `equal_nan=True`, while the variant tests ~840 coupling-state components at τ — so `R → A1′` measures **architecture plus stopping rule, summed**. Phase A knew this: `arms.py` states that **"R is a reference, not a competitor"** and Phase A accordingly never compared against R, building the control arm A0 for exactly this purpose. **Phase B compared against R directly, which is the mistake Phase A's own design documentation warns against.** The predicate term is not small — Phase A measured it at **−3.4 % to +8.6 %**, against a Phase B result of **+2.0 %**, so it can dominate the answer and possibly flip its sign. **It has already contaminated a published verdict:** all 13 starts the variant refused were refusals of non-finite intermediate state the baseline's predicate never examines — a predicate difference reported as an architecture property, with no control that could have attributed it. **The three arms:** **R** (PROCESS as shipped — the relevance anchor), **A0′** (flat fixed-point iteration on the coupling state at τ, in `caller.py` — the predicate-matched control), **A1′** (block + lift + hoist at τ). **`A0′ → A1′` becomes the headline** — architecture alone, at matched predicate and matched final accuracy — with `R → A1′` reported alongside as the user-facing number and `R → A0′` as the predicate's cost in production. **Three reasons this is cheaper than it sounds:** it solves the matched-accuracy problem directly, since A0′ and A1′ converge the same quantity by the same test and the exit audit finally compares like with like; A0′ is plausibly the degenerate case of A25's `module_solve.py` with one block containing every node, not a new solver; and it makes the robustness question decidable — if A0′ also refuses those starts it is the predicate, if not it is the architecture. Cost: one more arm across the multi-starts, ~50 % more run time on three decks. |
| **D19** | **The prime is part of the V3 intervention** (user, 2026-09-03/04, decided in session `process-surgery-bf` and confirmed in the orchestrating session). A35's carrier — `FirstWall` (M3) computes the run-constant pair `build.dr_fw_inboard`/`dr_fw_outboard` from two pure deck inputs; `Build` (M2, earlier in the schedule) reads the previous pass's values — is repaired by a **driver-level prime**: `PROCESS_ARCH_PRIME` executes `fw.set_fw_geometry()` at the head of every sweep so `Build` reads this pass's value. **Method-level, not a node reorder** (user framing: moving the whole `FirstWall` node ahead of `Build` would drag its physics-dependent reads upstream and create new backward edges); no `process/models/` edit; invocation stamped (`n_prime_calls`), never pooled into node-call counts. Switched on in **A1, B2, B3 only** (O4); the prime-free arm A1u carries the Phase A counterfactual; no prime-free Phase B twins (O5 — the ladder measures the whole intervention; G2 establishes post-call-1 bit-inertness). A38 measured the baseline the prime must close: restricted medians 6.4e-4 / 9.8e-4 / 1.15e-3, 25/25 pair-images on nof and st, one open term on lad (`tfcoil.m_tf_coil_superconductor`, G3c's). Naming history: "seed" → "preseed" → **"prime"** (2026-09-04, to end collisions with the perturbation seeds). Full construction: `V3_DEVELOPMENT_PLAN.md` §2 |

---

## Issue register

Open issues only. **Traps — recurring ways to be misled, as opposed to defects to fix — live in
[`../TRAPS.md`](../TRAPS.md)** and are binding on every task.

| # | Issue | Status |
|---|---|---|
| **I-1** | **The editable install pointed at the wrong tree** (`dev_libraries/PROCESS`, at superseded commit `710a75c9`), so runs in their own working directories imported the wrong code silently | **CLOSED** 2026-08-31 — conda env `PROCESS_surgery_env` created by the user; A1 re-ran every gate under it and confirmed **bit-identical** results to the `PROCESS_env` figures. `PROCESS_env` must not be used here |
| **I-2** | **`t_burn_0` is dead code.** `process/models/physics/physics.py:513` writes `times.t_burn_0` with a comment referring to "the convergence loop in `fcnvmc1`, `evaluators.f90`" — a file that no longer exists. The variable has no reader anywhere in `process/`. Evidence that burn time was historically *the* reconciliation variable, and a candidate small independent contribution upstream | **OPEN** — confirm no MFILE-path reader, then propose removal |
| **I-3** | **Superseded documents carried no staleness marking** and read as current | **CLOSED** 2026-08-31 — every one now carries a `> **Document status**` header naming the commit it describes and whether its numbers may be cited. The header distinguishes *stale* from merely *archived*, because `reports/deprecated/` holds both (trap T3) |
| **I-5** | **`st_regression.IN.DAT` was stale and did not solve at `c0ae5b28`** — archived from `710a75c9`, missing `i_tf_turn_type` | **CLOSED** 2026-08-31 by D9's patch: five keys copied verbatim from the base commit's own regression input, carrying `* D9 PATCH` provenance comments. Reproduces the base-input diagnostic bit-for-bit, so the patch is equivalent, not merely sufficient |
| **I-6** | **Two actors in one working tree put commits on the wrong branch.** Orchestrator admin commits landed on `A1-stage0-rebaseline` because the agent had checked it out in the shared tree. Repaired by fast-forwarding `architecture_surgery` (contiguous commits, no history rewritten). Root cause: the protocol said "own branch" where `PROCESS_code_analysis` says **isolated worktree** | **CLOSED** by the protocol amendment at §3 and §9 above — task work now runs in its own `git worktree` |
| **I-7** | **~~No free iteration-variable number exists.~~ Overstated — corrected.** `N_ITERATION_VARIABLES_MAX` is *derived* (`max(keys)`), not a hand-set cap, so appending 178 raises it automatically; arrays grow and `lablxc` self-populates. Nothing hardcodes 177. The real risks are **reusing one of the 94 gaps** (silently reinterprets existing `IN.DAT`s) and two branches independently picking 178 | **DOWNGRADED** — not a blocker; handled by D10 and the allocation table |
| **I-8** | **Wall clock is not measurable at the thresholds the plans use.** Worst within-arm spread **19.6 %** at `n = 5`; 2 SE on a difference of means 3–9 %. A 10 % effect is not resolvable, so the partition plan's stop rule sits *inside* the noise. **Probably contention, not the code**: 16 cores but 7 GB RAM, no thread-pinning set (OpenBLAS defaults to all 16 per process), other sessions concurrent — and the difference being far less noisy than the marginal is the signature of common-mode noise | **OPEN** — A18/F3 fixes the protocol: interleave arms and pair the differences, pin threads, record CPU time as a **diagnostic** (narrow CPU spread against wide wall spread confirms contention), log load and free memory per run. **Run the CPU-time diagnostic first** — it decides whether the rest is warranted |
| **I-9** | **The scenario deck was never under version control.** The repository-root `.gitignore`'s blanket `*.DAT` swallowed `arch_surgery/idf_probe/scenarios/*.IN.DAT`; upstream un-ignores its own decks by name and this one was never added. All four files existed only in one working tree, so **Stage 0 was not reproducible by anyone else**, and D9's "frozen artifact" premise was false as written | **CLOSED** 2026-08-31 — A1 added a scoped `!scenarios/*.IN.DAT` in `arch_surgery/idf_probe/.gitignore` (upstream's own pattern, without editing the shared root file) and committed the deck, 208 KB. Verified the un-ignore also covers *new* files. Audit of `arch_surgery/` found nothing else untracked |
| **I-10** | **Identical work varies by up to 35 % in CPU-seconds, and the cause is not known.** A2's diagnostic shows the runs are CPU-bound (cpu ≈ 99.8 % of wall) and the CPU-time spread tracks the wall-clock spread to two decimals (13.60 % vs 13.64 %; 34.76 % vs 34.77 %, `n = 5`), with all replicates producing one result signature. **This is not a contention signature** — scheduling contention would widen wall clock without widening CPU time. It points at frequency scaling, cache or memory-bandwidth effects, or the WSL2 layer. **Supersedes the earlier reading of I-8 as "probably contention", which this does not support** | **OPEN** — A18 must not assume the cause is known. A2 recommends `perf stat` IPC or thread pinning over `getrusage`. **Cross-checked with `PROCESS_code_analysis` (2026-08-31):** their suites show a ~16 % wall spread (388.5 / 388.4 / 349.9 / 407.5 s) that **correlates with background load** — a contention signature, which ours is not. **The two signatures differ**, so I-10 should not be assumed to be the same phenomenon. The discriminating measurement, requested of them: CPU-seconds alongside wall clock on a suite run. Also learned: their numpy pipeline likewise defaults to 16 BLAS threads with no pinning, so an overlap is ~32 threads on 16 cores against 7 GB — memory pressure is a more plausible mechanism than first credited. **Consequence adopted 2026-08-31:** wall-clock-derived weights are retired from any quantity a conclusion depends on — A2's measured-cost column included, retroactively. Acceptance is counts or bit-comparisons only (`CLAUDE.md`, working rules). **Diagnosis advanced 2026-08-31 by combining data with `PROCESS_code_analysis`:** their instrumented suite runs at **CPU/wall ≈ 1.03** — one core busy on a 16-core machine — and ours is CPU-bound at 99.8 % of wall, so also ≈ one core. **Two single-threaded workloads cannot contend for CPU scheduling on 16 cores**, which makes scheduling contention implausible for *both* projects and retires the "32 BLAS threads oversubscribed" hypothesis: whatever the unpinned defaults say, observed parallelism is ~1. Their max RSS is 1.30 GB against a 7 GB ceiling, so their suite is not a memory-pressure accomplice either. **Remaining candidates, in order:** memory-bandwidth or cache effects (shared even when cores are idle), host frequency scaling, the WSL2 layer. **The discriminating measurement is the CPU/wall ratio under load** — if it stays ≈ 1.03, CPU-seconds rose on identical work and no ratio derived from timings is safe on this machine. Not observable from inside the VM: `/proc/cpuinfo` reports a static nominal 2304 MHz, there is no `cpufreq` sysfs, and `/proc/stat` steal is 0 (though WSL2 may not implement steal accounting). **Cross-project baseline, 2026-08-31:** three quiet-window samples of the sibling project's suite gave wall 353.0 / 346.5 / 338.6 s, CPU tracking at ratio 1.03 throughout, RSS 1.27–1.30 GB. **I first read the descending sequence as a settling trend; that was wrong** — only samples 1 and 2 are identical content (353.0 → 346.5 s, −1.9 %, `n = 2`); sample 3 ran on a branch that had just deleted docstring prose across ~100 files, so it is structurally lighter import work and is not comparable. The monotone triple is therefore weak evidence of settling. What survives: `n = 2` says nothing about stationarity either way, so quiet repeatability on that suite is **unestablished**, not 2 % and not 4.2 %. **Practice adopted:** every timing carries its position in the run sequence **and a content hash** — the confound that produced this error. **Memory pressure narrowed further:** the sibling project's *heaviest* workload (full two-configuration pipeline regen) peaks at **0.47 GB** — smaller than its test suite, and 7 % of the 7 GB ceiling. Nothing they run approaches memory limits, so the memory-pressure mechanism now requires **our** workload to be the heavy party. **We have never recorded peak RSS** — a gap, and the cheapest thing to close: capture it in the quiet re-baseline. If ours is also small, memory pressure is out entirely and the candidates reduce to frequency scaling, cache/bandwidth effects and the WSL2 layer. **Closed on the memory branch, 2026-09-01:** captured as a by-product of D13's runtime check — `large_tokamak_nof`, warm, isolated subprocess, peak RSS **423 MB** (0.41 GB), against the sibling's heaviest **0.47 GB**. Both parties are small and comparable, and even together they are ~13 % of the 7 GB ceiling, so **neither is the heavy party and the memory-pressure mechanism is out**. Remaining candidates, now the whole list: frequency scaling, cache/memory-bandwidth effects, the WSL2 layer. **Cross-project loaded/quiet sample, 2026-09-01** (sibling study, content-identical code verified by a docs-only merge diff): QUIET wall 349.3 s / CPU 360.9 s / ratio 1.033; LOADED wall 398.5 s / CPU 412.5 s / ratio 1.035; RSS flat at 1.28 GB. **CPU-seconds rose ~14 % with the ratio pinned** — identical instructions consumed more CPU *time* under load; they did not queue. This is the same signature as our own 13.6 % / 34.8 % spreads (CPU tracking wall to two decimals) and it **confirms scheduling contention is not the mechanism**, since descheduling would widen wall while leaving CPU time flat and would drop the ratio. **First cross-project confirmation that the effect is present on their workload too.** *Three caveats, ours:* (i) `getrusage` CPU time is **time, not cycles or instructions**, so "more CPU for identical instructions" is exactly what *both* remaining candidates produce — memory stalls and a lower clock are indistinguishable in this metric, and separating them still needs `perf stat` IPC as A2 recommended; (ii) `n = 1` loaded, with a heterogeneous antagonist (their own git/docs work plus a background `git gc`, which is itself an I/O- and bandwidth-heavy load — consistent with the cache/bandwidth hypothesis, but not a controlled one); (iii) it shows **load causes CPU inflation**, which is a different claim from explaining our *unloaded* variance — it makes unrecorded background load a candidate for our spread rather than establishing it. **Decisive test agreed:** a quiet re-baseline of ours collapsing to ~2 % would close the case that the machine, not either project, owns this. **Deliberately not run yet — M100 is executing on the sibling side**, so the machine is not quiet and a re-baseline taken now would be contaminated by construction. Take it after M100 delivers, with load average, RSS, content hash and sequence position recorded. **Idle-window protocol agreed 2026-09-01:** the sibling study sends an explicit *"idle window open"* with what last ran and when, once its dumps, pipeline runs, gate suite and merge are done and nothing of theirs is queued; we take the re-baseline in that window and not before. The same protocol covers a `perf stat` IPC run — the one measurement that would separate cache/bandwidth from frequency scaling — which **neither project runs uninvited on a shared host, and which needs the user's word.** **Idle window opened 2026-09-01** after M100 merged. **Still not taken, on two independent grounds:** the user deferred I-10 as not relevant to the revised experiment (Phase A's acceptance quantities are counts and bit-comparisons, unaffected by machine load); and our own A18 agent was running PROCESS at the time, so the machine was not quiet **from our side** — an idle window on the sibling's side is not an idle machine. **Third under-load datum, same session:** their 2 050-test gate suite ran *while* our two agents ran PROCESS — wall 441.0 s, user+sys 481.8 s, ratio pinned ~1.05, RSS 1.28 GB, per-test wall **+28 %** against quiet. *Caveat, ours:* that run was **not content-identical** (+34 sub-second tests), which is why it is normalised per-test — a weaker comparison than their earlier content-identical pair, and it should not be quoted as if it were the same class of evidence. The issue stays **OPEN** — a mechanism has been eliminated, not identified |

---
| **I-11** | **A task worktree was seeded from upstream `main`, not from `architecture_surgery`.** A20 (registry-append)'s isolated worktree was created at `6df46205` — upstream `main`, not an ancestor of this project's branch, with no `arch_surgery/` directory present at all. The agent detected it and created its branch at the correct commit, so no work was lost | **STRUCTURALLY RESOLVED 2026-09-01** (user: "do 2"). **Root cause, measured:** the harness seeds a worktree from the repository's **default branch**, not from the current checkout. `main` *is* `6df46205` and `refs/remotes/origin/HEAD` points at it, while the main checkout's `HEAD` has been `architecture_surgery` throughout. **7 of 7** worktrees this project ever created landed on `main` — so this was never intermittent, and **A18 was not the exception recorded here earlier**; it was mis-seeded too and its agent corrected course unnoticed. That earlier reading is withdrawn. **The compounding problem:** `main` carries no `CLAUDE.md`, no `arch_surgery/` and no `TRAPS.md`, so a mis-seeded agent starts with **none** of this project's rules loaded. The guard cannot live in the tree it guards, which is why detection had to move to the dispatch side. **The fix:** the orchestrator creates the worktree itself with [`../../bin/new_task_worktree.sh`](../../bin/new_task_worktree.sh) and dispatches **without** harness isolation. The script branches at the `architecture_surgery` tip and asserts the tree the agent will actually get — named tip, descent from `c0ae5b28`, **not** upstream `main`, and `arch_surgery/` / `CLAUDE.md` / `TRAPS.md` present — then **tears itself down if any assertion fails**, so a wrong tree is neither handed over nor left behind to block the next attempt. Both negative paths are tested (duplicate branch; `main` as start point). **Note for anyone writing a similar check: "descends from `c0ae5b28`" is worthless alone** — upstream `main` descends from it too, so that test passes on precisely the tree it exists to catch; only the named-tip and tree-presence checks do any work. **Residual risk, stated not closed:** the harness path still mis-seeds if anyone dispatches with `isolation: "worktree"`, so this is a convention the orchestrator must follow rather than a mechanism that forbids the mistake. `CLAUDE.md` carries the backstop — *if your tree has no `arch_surgery/`, you are on the wrong branch; stop and say so* — which is the one instruction that survives in a mis-seeded tree, because it travels in the brief rather than the tree. **Superseded detail, kept because it is still true:** `run_one.py`'s provenance banner (tree, branch, commit, dirty marker, and a warning when the tree does not descend from the base) covers measurement runs, not worktree seeding |
| **I-12** | **A relative convergence test scaled by a median magnitude becomes arbitrarily tight where a quantity diverges — and PROCESS's 1990 cost model diverges at infeasible points.** A22 found 7 of `st_regression`'s 144 design points needing 4–7 outer passes where every other point needs 2. On 6 of the 7 the entry state has **negative** net electric power (−1.6 to −3.0 MW against a deck median of +110 MW), which sends `costs.coe` to 6.6 × 10²¹ against a characteristic scale of 1 251 — so **our** scaled test at those points is roughly **10¹⁸ times tighter than intended**, and the loop iterates until the state is bit-identical. **Attribution corrected 2026-09-02 (A27), and it matters:** the tightening is a property of **our** median-scaled predicate, **not of upstream PROCESS's**. `np.allclose` is relative to the *current* value, so upstream's tolerance at `coe = 6.6 × 10²¹` is 6.6 × 10¹⁵ absolute — **looser than ours by 5.3 × 10¹⁸**, verified. A large-but-stable `coe` costs upstream nothing. The upstream-facing finding is the **divergence itself**, which A27 traced to `costs.py:2751`: `max(kwhpy, 1.0e-10)`, a guard written against `kwhpy = 0` applied to a *negative* `kwhpy`, turning the division into ×10¹⁹ (`1e9 × 650 / 1e-10 = 6.5e21`, matching the observed `coecap`). The seventh point enters feasible and reaches the same region during outer pass 1. **This is not coupling** — all three fields (`coe`, `coecap`, `coefuelt`) are written by `costs` in the feed-forward tail, downstream of every module — but it inflates an outer-pass count, which is an acceptance quantity | **OPEN, and it will recur in Phase B.** D15's multi-starts perturb the start point *deliberately*, so infeasible entry states are not an edge case there but the design. An arm that visits more infeasible starts would show more outer passes for a reason that has nothing to do with architecture. **Consequences adopted:** (i) Phase B records net electric power at entry per start and reports the count of degenerate starts alongside every cost figure, as A22 did with its 7/144; (ii) a start whose convergence is driven by a diverging tail field is flagged, not silently averaged in; (iii) the feed-forward hoist removes the mechanism entirely — A18's `hoist = 1` variant takes `st_regression` to {1: 9, 2: 135}, mean 1.9375, with all seven long points gone — which is a *reason the hoist is inside the Phase B variant* (D15) beyond its own cost saving, and must be stated as such rather than folded into the headline. **Owed to `PROCESS_code_analysis`:** the divergence of the 1990 cost model at negative net electric power is an upstream robustness observation of the same class as the `equal_nan` finding already filed; send it with the 7/144 denominator and the magnitudes |
| **I-13** | **Phase A's hoist and the production hoist do not agree on one deck.** `arch_surgery/fixedpoint/arms.py`'s `hoisted_nodes()` has **no figure-of-merit guard**, so on `large_tokamak_eval` it hoists `['water_use', 'costs']` while A13's production hook resolves `['water_use']` alone. A13 found why the guard is needed: `objectives.py` reads `costs.coe` (FOM 6) and `costs.cdirt`/`concost` (FOM 7), and that deck takes the default FOM 7. **Neither is a bug in its own setting** — A18's engine converges on the measured coupling state `y`, not on PROCESS's `objf`/`conf` predicate, so hoisting `costs` there does not corrupt its convergence test the way it would in the driver. But it does mean **`hoist = 1` in Phase A and `PROCESS_ARCH_HOIST=feedforward` in Phase B are not the same architecture on that deck**, and any document putting a Phase A hoist number beside a Phase B one owes the reader that sentence | **OPEN.** A13 recorded it and deliberately did not change a merged instrument — the right call. **Consequences:** (i) `large_tokamak_eval` is the deck this affects, and it is already the weakest of the four (A22: 555 of 840 coupling components classified constant from a 10-point harvest), so the cheap resolution is to say so rather than to re-run anything; (ii) **A21 must not put Phase A and Phase B hoist figures in one column for that deck without the caveat**; (iii) if Phase B ever needs them commensurable, the fix is to give `hoisted_nodes()` the same guard and re-derive — a replay, not a solve |
| **I-15** | **`retire_task_worktree.sh` destroyed A29's run artifacts — I-14's failure mode recurring through its own fix.** The script's collision rule was *skip-and-continue*; the forced worktree removal then deletes whatever was skipped. A29's `runs/a28/` (its 375-run replication campaign at the clean commit, calibrate, gate, audit artifacts) collided with the main tree's `runs/a28/` (A28's originals) and was skipped, then destroyed. **Loss bounded, and materially smaller than I-14's**: the report and both verification summary JSONs survive (`A29__verification_phase_a.json`, `A29_phase_a_report.json`), the orchestrator's independent provenance check (375/375 records at `b3c4d131`, dirty=False) was made **before** retirement and is recorded in merge `4dade3b0`, and — unlike I-14 — everything lost is **regenerable by the committed entry points** (~4 h at A29's measured rates), which regenerability was A29's own finding. | **CLOSED for recurrence 2026-09-03**: collision now namespaces with the task branch (timestamped if needed) and never skips; only symlinks into the main tree are skipped, since moving those would shadow the shared recording. The deeper lesson joins I-14's: *any* path where `git worktree remove --force` runs after a conditional `continue` is a destruction path and must be audited as one |
| **I-14** | **A26's run artifacts were destroyed when its worktree was removed.** `git worktree remove --force` deletes untracked files with the worktree, and run artifacts under `arch_surgery/idf_probe/runs/` are untracked **by design** (`CLAUDE.md`). The orchestrator removed A26's worktree without relocating them first, destroying `runs/a26/` — including `matched_accuracy.json`, the evidence behind the correction that **overturned Phase A's published headline**. An earlier cleanup on 2026-09-01 had done this correctly, by hand, moving ~1.4 GB across before removing seven worktrees; the failure was relying on remembering | **CLOSED for recurrence 2026-09-02**, and the loss is **bounded but real**. *What survives:* the archived report with every table and denominator; the merged code (`run_a26.py`, `a26_*.py`, `accounting.py`), so the numbers are **reproducible**; and the orchestrator's independent re-derivation of the envelope, recorded in the decision log and the results banner — the bit-identical `1.2556721063507803e-11` at 9 062 against 9 471 model evaluations. *What is gone:* the raw ladder, gate and timing artifacts, so a future check must **re-run rather than re-read**. Not regenerated now because A28 holds the heavy slot and its own matched-accuracy work supersedes A26's for the three retained decks. **Prevented by** `arch_surgery/bin/retire_task_worktree.sh`, which relocates artifacts, refuses to act on an unmerged branch, namespaces to avoid collisions, and only then removes the worktree and deletes the branch — the teardown counterpart to `new_task_worktree.sh` |

## The queue

Rows are filed by **state**, not by number. A task appears in exactly one table.

### Live chain — everything else waits on this

`A28` is assessed and merged first; `A30` and `A29` may then run beside each other.

| # | Task | Prereqs | Status |
|---|---|---|---|
| **A28** | **phase-b-rerun** — re-run Phase B on A26's fixed instrument, **three decks** (D17 drops `large_tokamak_eval`), then deliver Phase B **in the same two-part form Phase A has**: **(a) `MDA_partition_opt_experiment.py`** at the repository root — one runnable entry point that reproduces the Phase B results end to end, named and structured to sit beside A21's `MDA_partition_experiment.py`, sharing its runner rather than duplicating it, with the same concise jargon-free docstring for a reader who has not followed the queue (user, 2026-09-02); **(b)** the Phase B results written into `MDA_partition_exp_results.md`, replacing its §7 "not built, stated as a gap" and extending §§5–6 to cover both phases, with the abstract and conclusion updated. **Authored against the fixed instrument, not the superseded one** — A25's run is documented by its own archived report and its artifacts, and is not what this entry point should reproduce **Runs three arms, not two (D18):** **R** PROCESS as shipped, **A0′** the predicate-matched flat control, **A1′** the variant. **`A0′ → A1′` is the headline**; `R → A1′` is reported beside it as the user-facing figure and `R → A0′` as the predicate's own cost. Build A0′ as the single-block degenerate case of `module_solve.py` if that holds, and say so if it does not. **Re-attribute A25's 13 refused starts against A0′** — that decides whether the robustness deficit is the predicate or the architecture, which the two-arm design could not answer | A26, A27 | **MERGED 2026-09-02 (0c18dfcc). The verdict moves: the architecture wins on one deck, is inconclusive on one, and turns on a single start on the third.** `A0′ → A1′` at matched stopping rule: **−1.63 %** on `large_tokamak_nof` (20/22 cheaper, q1–q3 0.982–0.985, **identical success sets**), **−6.18 %** on `st_regression` (robustness net −1 of 25 at fixed τ, **23-vs-23 at matched accuracy, −2.27 %**), −21.2 % over 8 usable starts on `low_aspect_ratio_DEMO` (**inconclusive** by the pre-declared rule). **Differs materially from A25's 'does not win anywhere', for measured reasons:** A25's comparison folded in the stopping-rule term (`R → A0′` is +2.13/−3.38/+3.23 % — comparable to the architecture term and not of one sign); **two thirds of the refusal deficit is the predicate, not the architecture** (the coupling-state test refuses only on `low_aspect_ratio_DEMO`: variant 3 starts, control 2 of the same 3, same quantity — the measurement D18 added the third arm for, verified by the orchestrator from raw metrics); and **the variant now solves a start the control cannot** (`st_regression` start009, control `ifail=2`, verified). §7a honoured: the hoist alone is −4.39/−2.96/−4.32 %, the architecture **without** it costs **+2.88 %** on `large_tokamak_nof`. **It also flipped Phase A's sign on two decks**: the matched-count envelope (one knob each) gives **+33.4/+27.4/−15.2 %** where all-settings gives −4.3/−4.5/−13.1 — construction, not reproduction (A26's numbers reproduce to rounding); both constructions now published, the honest object the bracket. Protocol §12 caught a defect in the agent's own gate — **fifth consecutive task** (`_SEQUENCE_HEADS` is an `AnnAssign`; three head models missing from the call-site set while the gate passed). §15 followed: both entry points executed end to end (`--quick`), `--verify` 3/3 decks, Phase A smoke reproduces A18's counts from a fresh harvest. **Largest open caveat, stated in the report (AD4):** Phase B ran on A18's coupling-state artifact, not A26's no-exclusion one — on `st_regression` 24.2 % of control solves vs 19.3 % of variant solves have a 'constant' moving inside them, so its −6.18 % is **provisional**. Campaign metrics carry `9634bb06-dirty`; full-scale from-clean tie-down is A29's |
| **A30** | **phase-b-critique** — **the adversarial pass over A28, run by an agent that did not produce it** (user, 2026-09-02: *"do a critical evaluation of the results, as was done before for phase A. Check for methodology errors and systematic bias by the agent. I need to be sure we make a fair comparison that isolates the effect of the architecture changes"*). Phase A's equivalent found **three errors in A18's merged report** and A26 **overturned its headline**, so this is the norm, not a formality. The standing question: does `A0′ → A1′` isolate **the burn-time lift plus the feed-forward hoist**, or does it carry other work? Checks fixed in advance, each to be answered with a number and a denominator: **(1) Factor identification.** `env_for` makes `A0′ → A1′` vary **four** things — block partition, the `build`/`physics` transposition, the lift, the hoist. `A0p_reordered` isolates the transposition and `A1p_nohoist` the hoist, but **no arm isolates the lift**: there is no `per_module + hoist, no lift` and no `flat + lift`. Since the lift is half of the user's stated independent variable, either measure its own share or state plainly that it is not separately identified. **(2) Envelope selection bias.** The block arm gets **9 rungs against the flat arm's 5**, and cost is read off `min` over settings, so it has more draws at the minimum. `a28_analysis.ladder` declares this "bounded rather than removed" — **check what the bound actually is**. Two one-sided biases, both favouring the block arm: (a) a running minimum can only fall with extra draws, and the block arm's four extra rungs all sit at joint `tau = 1e-6`, so the extra sampling is **concentrated** in one accuracy band rather than spread; (b) the envelope is interpolated linearly in log-cost against log-accuracy, and a chord across a convex curve lies **above** it, so the arm with wider gaps — the flat one — is over-charged. **The matched construction needs no new runs:** compare the block arm's **five joint rungs** against the flat arm's five, same knob and same tau values, and report it beside the all-settings envelope. If they differ, that difference is a **tuning premium** — two knobs against one — and gets its own number rather than being folded into the architecture's headline. Note A26's own analysis flipped sign on an envelope-construction choice (+21.9 % → −4.3 %) against a ~4 % effect, so the construction has leverage comparable to the measurand. **This must appear in the standing document, for both phases** (user, 2026-09-02: *"this is a sensible approach to deal with the envelope issue. It should be addressed in the reports"*) — a bias declared in an analysis JSON is not a bias addressed; the report is what is read. A30 confirms the treatment is there and is honest. **(3) The `n` vs `n+1` cost is CHARGED, not corrected.** *(Settled with the user, 2026-09-02: the extra design variable is part of the intervention, not a confound — lifting inherently buys its decoupling by enlarging the design vector.)* So the check inverts: confirm the extra finite-difference sweeps are **inside** the cost unit and nothing excludes them. Deck fairness is already verified by the orchestrator — the lifted decks differ from the frozen ones in **exactly three ways** (`icc = 93` inside the equality block, `neqns` + 1, `ixc = 178`), the burn time's initial value is the **baseline's own settled value**, measured not chosen, and the perturbation factor is hashed on `(seed, ixc number)` so every shared design variable takes an **identical** perturbation in both arms. Re-verify at A28's final commit, then treat as closed. **(4) The three decks are not the same treatment.** `st_regression` is **not** in `PULSED`: `k = 0`, no lift, `HOIST=feedforward` not `feedforward_lifted`. Any figure spanning decks mixes two different interventions. **(5) Accuracy statistic.** A26's matched-accuracy result rested on **p90**, the median exit residual being exactly 0 on 15 of 17 rungs and the arms indistinguishable on the worst point (0.82–1.23). Check whether A28 inherited that fragility and whether the sign moves under a different statistic. **(5a) Robustness is compared at MATCHED TOLERANCE, not matched accuracy** *(user, 2026-09-02: "why is the tolerance not set at the beginning of the campaign, such that the results are comparable by nature?" — the question exposes the asymmetry rather than proposing it)*. The ladder fixes **cost** at matched achieved accuracy, but `jobs_campaign` runs every arm at fixed `TAU = 1e-6`, so success rates and refused starts — **the quantity the verdict actually rests on**, since robustness outranks cost in the declared outcome table — are compared on exactly the basis A26 showed flips the sign of the cost number (+21.9 % → −4.3 %). Confirm from the ladder's own points whether A0′ and A1′ achieve the **same** achieved accuracy at `tau = 1e-6`, per deck with denominators, and report the per-arm accuracy **distribution** across the 25 starts rather than the median — robustness is a tail property. If a material gap exists and A28 did not re-run the affected deck at matched accuracy, that re-run is A30's. **(6) Drop census before any ratio** — crashed, refused and non-converged starts counted per arm, and any asymmetry in what was dropped named. **(7)** Re-derive the **13 refused starts** attribution independently; A25's negative verdict rests on it. **(8)** Confirm the §12 gate-teeth evidence covers the arms actually used in the headline. **Read-only on results**: this task re-derives from A28's artifacts and may run its own checks, but does not edit A28's numbers — disagreements are reported as findings | A28 ✓ | **MERGED 2026-09-02 (7ef7d2a1), corrections applied (e63bab8a). A28's arithmetic holds throughout — every re-derived table agrees to the digit from 870 raw records — and five findings were real.** F1 (T11 class): the standing document's 'starts not solved' column counted **crashes only** and published 0/0/0 on `st_regression` where the raw records give 1/2/3 not-solved of 25, contradicting §7.6 in the same document; relabelled, refusal conclusion survives. F2 (check 1's answer): **no arm pair varies the lift alone** — `A0′ → A1′_nohoist` varies grouping *and* lift, so the quoted +2.88 % is **partition-plus-lift**; three merged statements relabelled. F3: the raw runs record **four commits, most dirty**; bounded from artifacts (6/6 repeated configurations bit-identical in cost and `norm_objf` hex across three heads; nothing after `9634bb06` touches `process/`), not closable without A29's from-scratch run; provenance note added. F4/F5 minor (24+1 starts, not 25 perturbed; a stale improvement item). **The loose ends all resolved cleanly**: the acceptance rule is D15(c), pre-declared and symmetric, both readings moving the headline ≤ 0.6 pp; the 'sixth rung' dissolves (Phase A's ladder is six taus per family, Phase B's five, both same-knob); the headline row is one declared rule identical for both constructions and mid-range, not adverse-selected; AD4's contamination moves −6.18 % by ≤ 0.07 pp under every artifact-derivable restriction (the no-exclusion re-run stays A28's top successor item); Phase B's verdict is statistic-invariant (p90/p50/max) while Phase A gives **no read at all under p50**. Its own checks passed §12 with 10/10 teeth on doctored input; no PROCESS solve run |
| **A29** | **replication-verify** — **run both replication scripts from clean, as a user would, and confirm they reproduce the published numbers** (user, 2026-09-02: *"I want to rerun both phase A and phase B results after you're done, to verify the numbers myself. Ensure that the replication scripts are constructed right"*). Not a review of the code — an **execution** of it: `MDA_partition_experiment.py` and `MDA_partition_opt_experiment.py`, from a clean checkout, in a fresh environment, with nothing pre-seeded except what the scripts themselves build. Confirm per deck with denominators that the output matches the results document; confirm determinism by re-running at least one stage and comparing bit-for-bit; record the **actual wall-clock and disk cost** of each so the user can plan. **Any divergence is the deliverable**, not a defect to quietly repair — if a script does not reproduce a published number, that is the finding. Also check the failure paths: wrong conda environment, absent input deck, missing cached artifact, and `arch_surgery/idf_probe/runs/a26/` being **gone** (I-14) — each must fail with a message naming the fix | A28 ✓ | **MERGED 2026-09-03 (4dade3b0). The published numbers reproduce from clean: 30/30 deck-level comparisons** (18 exact counts/booleans, 12 ratios within the published rounding, worst 4.5e-6) across all five Phase A and five Phase B published tables; the calibration table 9/9 cells **including which starts crash**; determinism bit-identical over 800k+ compared leaves/lines, every diff classified as metadata. **All 375 campaign records provenance-stamped at one clean commit — closes A28's dirty-tree caveat (A30 F3) by measurement, verified independently by the orchestrator.** The answer was "no" until two from-clean defects were fixed by execution: the driver treated any failed sub-run as fatal and halted at calibrate on the two crashes the published table itself records; and Phase A `--verify` silently dropped one of five tables (present at A28's merge — its "3/3 on every table" was 4 of 5). §12 caught the task's own comparator selftest — **sixth consecutive task**. Measured costs: `--quick` 12+7 min; full Phase A ≈ 68 min / 0.86 GiB (the published ≈ 6 GB is wrong — F8); executed Phase B ≈ 3 h / 5.6 GiB. **Descoped by the user 2026-09-03**: the ladder + matched-accuracy replication (≈ 1 h) deliberately not run — §7.8's matched figures are reproducible-in-principle, unverified, stated loudly. Trip-ups for a human run: ~1 h shell kills (stage-level `--resume` works, exercised twice; the entry point cannot pass it through), and the wrong conda env is **neutralized, not refused**, by the entry points |

| **A31** | **drift-diagnostic** — name the component behind `st_regression`'s recurring 3+-pass drift (user, 2026-09-03: *"launch the diagnostic agent in parallel already"* — explicit authorisation to run beside A29's heavy slot, small run budget). Per-pass tracing behind a new env switch in the driver (switch-neutrality gated per §12), then a handful of A1′ runs at the heavy-tail starts (`start010`, `start005`), recording per call, per pass ≥ 2: the argmax component, every component > τ, and each one's writer. Classify against the static census: the only cross-block loop-carried edge on this deck (`FirstWall → build.dr_fw_*` → Build) is **provably frozen** (loader-only inputs), so the expected verdict is a **non-idempotent model** — a member of the dependency instrument's declared 498-variable unmeasured-timing population (V14). Any cross-block pair found instead is a static-analysis miss and goes back to the sibling as the concrete case | A28 ✓ | **MERGED 2026-09-03 (3a75b264). Verdict: NEITHER hypothesis — the recurring tail is `pf_power.srcktpm`, a harvest-constant flickering by exactly 1–2 ULPs at hostile states under the predicate's no-tolerance exact-equality assertion** (reset-and-accumulate at `power.py:352/411`, recomputed each sweep from sub-τ inputs, landing on adjacent doubles: every recorded move 2.274e-13 or 4.547e-13 kW, max relative 4.11e-16, fingerprint in 25/25 starts; 1,293 of start010's 1,294 failing pass-≥2 tests carry **nothing above τ** — verified by the orchestrator from raw trace records). Hypothesis A refuted (largest τ-tested residual 13× under τ; `a_tf_plasma_case` is merely the sub-τ argmax on 89 % of failing records, explaining the ladder-audit attribution innocently); hypothesis B refuted for the recurring class (`pfpwr` stateless per sweep). **The architecture is innocent; the flat control pays the same flicker plus `costs.c22524`** (in-loop only there) — AD4's 24.2 %-vs-19.3 % contamination asymmetry now has its mechanism. **Under the committed a26-mode spec the flicker scores 4.1e-16 and the tail dissolves by construction** — derived, one confirming campaign run left to V2, which also restores trust-mode's viability on `st_regression`. Neutrality gate PASS with 3/3 teeth, bit-exact against A28's record. **No upstream defect and no DSM defect: the misclassification was our instrument's** (A18-mode spec), which is why nothing routes to the sibling or upstream — V14 closed as RESOLVED (follow-up 4) |

| **A32** | **tail-confirm** — the confirming run A31 left derived (user, 2026-09-03: *"Confirm that the tail vanishes"*): re-run `st_regression` A1′ across all 25 starts under the **a26-mode spec** (`ystate_a26_st_regression.json`) and show the recurring 3+-pass tail dissolves — A28's recorded 2,802 calls at 3+ passes should collapse to ≈ 25 (the cold first call, once per run). Plus the three heaviest A0′ starts for the flat arm's flicker. Report per start: outer-pass histogram vs A28's recorded one, `ifail`, `norm_objf` agreement within the gate rtol, and the cost delta as context. **Not the headline re-run** — no cross-arm ratio is recomputed; that stays V2's | A31 ✓ | **MERGED 2026-09-03 (637a6bb6). CONFIRMED — the tail vanishes: 2,802/54,480 → 25/49,920**, per-run tail exactly [1]×25, the survivor verified **by call index** (traced start010: 1 of 12,930 calls at 3+ passes, call 1, converging `fwbs.p_cp_shield_nuclear_heat_mw`, a genuine continuous — not flicker). Moved-constant counter **0 in both arms** (A1′ 10,528→0; A0′ heaviest-3 9,093→0 — A0′ has *no* 3+-pass tail anywhere in A28, its flicker lives in that counter; the brief's "heaviest A0′ tails" was A1′'s ranking, corrected). En route the task found **the committed driver had never loaded an a26-mode spec** (loader mode-blind at `module_solve.load_spec`; no a26-generation write set) — stop-and-report at `e8915f40`, then lifted under the driver-scope rule at `99fefded`: mode-aware `load_spec` (gated bit-exact for A18 artifacts, 3/3 fields + 3/3 teeth), `a25_writeset.py --spec-variant` (control regenerates the committed A18 write set exactly). Same pairing gap exists on the other three decks — V2 Appendix A. Cost deltas (54,480→49,920; 29,490→16,530 on a hostile 3-start sample) reported as observations only. Report: `deprecated/A32_tail_confirm.md` |
| **A33** | **post-solve-hoist instrument** — the V2 intervention component decided 2026-09-03 (user: FF nodes whose outputs the optimiser never consumes leave the per-call path, both phases; see `MDA_partitioning_experiment_v2/EXPERIMENT_PLAN.md` §1 and Appendix A 3a). **(i) Classification artifacts:** per deck, committed `postsolve_<scenario>.json` from a backward crawl of the sibling's collapsed DSM export (read-only) seeded by the objective's and active constraints' read-variables (per `minmax` from `objectives.py`, per `icc` from `core/solver/constraints.py`), confirmed in source, V6 branch-liveness checked per deck. First-cut to verify, not assume: `costs` + `water_use` out on all three decks; `pulse` stays per-call on the pulsed decks (fom −14 reads `times.t_plant_pulse_burn`, `objectives.py:81`; icc 13 reads burn time). **(ii) Driver capability:** env-switched `PROCESS_ARCH_POST_SOLVE` exclusion of listed nodes from solve-phase `_call_models_once`, one execution at the accepted optimum before output; byte-neutral when unset — **gated** against A28's record, teeth. **(iii) Correctness gate:** at fixed x on sampled starts, constraint vector + objective **bit-identical** with and without the exclusion, per deck, teeth. Driver scope only; no `process/models/` edit. §15: committed scripts, failure paths reachable | A32 ✓ | **MERGED 2026-09-03 (4cf488f6).** Classification overturned part of the first cut, crawl winning as designed: **`vacuum` leaves on all three decks** (readers: the post-solve cost model + availability branches dead under `i_plant_availability = 0`), **`pulse` additionally on `st_regression`**; pulse per-call on the pulsed decks (fom −14 / icc 13 / icc 93 read burn time); availability stays (its `costs.bktcycles` is a direct icc-90 seed on lad). Driver: `PROCESS_ARCH_POST_SOLVE` with sha/deck/node refusals (teeth 4/4). Gates PASS and **re-executed clean** (`c4540b38`) after the orchestrator caught dirty-stamped runs: neutrality 3/3 bit-exact vs A32; **full-run equivalence 12/12 fields bit-identical per deck**, node ledger reconciling exactly. Measured saving at baseline: **8.34/8.35/11.33 %** of solve-phase node calls (vacuum was loop-resident, ~3.4×/call). Pulsed a26 write sets delivered (controls 4/4). Report: `deprecated/A33_postsolve_hoist.md` |
| **A34** | **phase-a-instruments** — the three remaining V2 instrumentation gaps (`EXPERIMENT_PLAN.md` Appendix A items 2/3; `v2_config.INSTRUMENTATION`): **(i) trust mode** — env-switched no-outer-loop for `per_module` block solves (Phase A BLOCKS, Phase B A2): one pass of the block schedule, no outer predicate evaluation; gate: env unset ⇒ bit-identical to A32's recorded a26 start000, env set ⇒ runs with `outer_pass_hist` all 1s; teeth. **(ii) pin instrument** — Phase A pulsed decks: burn-time coupling held at an env-supplied value through a feed-forward block solve, value stamped in metrics; equivalence gate: pinned at the FLAT arm's converged value, the chain must reproduce the FLAT fixed point within audit resolution (plan §3). **(iii) single-MDA-eval mode** — run ONE `call_models` under the arm's architecture at a coupling-state-perturbed point (seeded ±δ keyed on component NAME), no optimiser, counts + uncharged a26 exit audit, then stop; Phase A's entry point for both arms. Driver + harness scope; **no `process/models/` edit; do NOT touch post-solve (A33's, concurrent — keep caller diffs additive, A33 merges first)**. §12 teeth on every gate; §15 committed scripts | A32 ✓ (A33 for merge order only) | **MERGED 2026-09-03.** All three instruments built, env-switched, every run clean-stamped `048c3aa3`: trust mode (`PROCESS_ARCH_OUTER=trust`, neutrality 3/3 bit-exact + teeth, demo hist all-1s), pin (`PROCESS_ARCH_PIN_BURN_TIME`, lift-required, ixc-178 refusal, per-sweep bit tripwire), single-MDA-eval (`v2_eval_one.py`, 4/4 bit-exact vs A28's call-1 record; perturbation stream 799/799 bit-identical across arms). **Two design-relevant findings:** (1) `pin_gate` FAIL-as-bound with localisation — a one-pass chain at a **cold entry** sits 1.46e-2 from the fixed point while the same pinned chain with the outer loop reproduces it at 1.5e-8 in 3 passes; not the pin, not pulsed-specific → Phase A entry-state design decision (warm-entry recommended; also 767/799 continuous components are zero at cold init, gutting a multiplicative stream). (2) Trust-mode end-to-end demo on one baseline trajectory: iterations 10→20, node calls +46 % — interior accuracy feeds gradients; measured caution against R1's "pure receipt", campaign judges. Report: `deprecated/A34_phase_a_instruments.md` |
| **A35** | **cold-entry runtime census** — the carrier of the cold-entry gap (A34's pin_gate: one-pass chain 1.46e-2 off the fixed point; verified control repairs it in ~2 extra passes — too fast for any slow intra-block mode, so the deficit rides cross-block structure). **Dynamic first** (the user's ruling; precedent: A31's static scan already failed to close this class, and the probe instrumentation exists because static read-tracing is untrustworthy here): traced **verified** chain from the cold entry on `large_tokamak_nof` + `st_regression` (a26 artifacts), pass-≥2 movers classified per component — mover's block vs writer's block (backward channel, named to the edge) vs bit-identical inputs with changed output (non-idempotent model); **flat-arm control** from the same entry for symmetry. Static liveness-at-initial-values scan only as candidate generation if the trace points at an edge; sibling handoff only with a demonstrated defect (standing rule). **Not blocking the V2 campaign** (user, 2026-09-03): feeds the mechanism sentence, B3's hostile-state validity bounds, and first-call accounting | A34 ✓ | **MERGED 2026-09-04 (merge `b20b6112`).** Carrier named, primary label **KNOWN-CUT** — no artifact wrong anywhere: `FirstWall` (M3) writes `build.dr_fw_inboard`/`dr_fw_outboard` (`fw.py:347-352`, a run-constant of two pure inputs — **V3's edge**), `Build` (M2, earlier in the executed schedule) reads them (`build.py:836/840`, `:1940-1947`). Fixed-point-dead, displacement-linear (δ-ratio ≈ 2.00), a **depth-1 transport delay** repaired in ONE verified pass (pass-2 1.459e-2 → pass-3 8.3e-16; the earlier "~30×/pass" was that one-shot repair averaged over passes). Coefficient-exact closure both decks (0.5·(Δin+Δout) / −Δout; rel diffs ≤ 7.4e-8); candidates (a) missing edge, (c-order) and (d) non-idempotency excluded by measurement (restart chains bit-identical 840/840, 827/827); zero M1/PULSE movers; st-export known-cut census returns exactly this pair. 19/19 runs ok, 4/4 gates PASS with teeth; plan committed before execution with two dated user-directed amendments. **Orchestrator recheck ALL PASS** (`a35_recheck.py`; report §9; 243/244 near-τ operationalization spread documented). Scope: `low_aspect_ratio_DEMO` not run (campaign owned the machine); ~0.24 Phase A audit reconciliation deferred to the V2 tally as a hypothesis. Register: **V15**. Report: `deprecated/A35_cold_census.md` |
| **A36** | **phase-a-campaign** — implement Phase A's campaign and tally on A34's instruments under the **warm-entry design** (user decision 2026-09-03): per deck, (1) reference = A0-arm single-eval at the cold deck point (one `call_models` under `flat_state` = the converged flat state; its cost recorded as the once-per-run cold-start term), exact exit snapshot kept; (2) **warm equivalence gate**: A1-arm (B3 per-call structure: resequenced per_module + trust + post-solve + lift/pin on pulsed) from the reference snapshot, unperturbed, pin = reference burn time — audit vs reference must be τ-grade (this supersedes A34's cold, A18-labelled pin_gate), teeth; (3) campaign: seeds 1–25, multiplicative ±δ perturbation of the snapshot (components non-zero at the warm reference, so the cold-entry zeros problem does not arise), seed-paired identical entries across arms, pin per-seed = the perturbed burn-time value; (4) tally: per-node counts, weighting-invariance bracket, audit similarity (F=10, median+p90), lift residual separate. Needs `v2_eval_one.py --entry-state` (restore_snapshot exists — A34 built the loader); extension gated: A0-arm from its own unperturbed snapshot must audit ≈ 0, teeth | A34 ✓ | **MERGED 2026-09-03 (3dd2bb96).** Extension gate PASS 7/7 (827/827 bit-identical warm re-entry, audit 1.1e-10, teeth bind); **warm equivalence gate PASS** (cross max 3.3e-9 — bit-identical to the reference's own one-more-sweep audit, 0/827 ≥ τ, teeth 2/2) — supersedes A34's cold pin gate; pairing 2/2 seeds × 827/827; perturbation reach **751/805** at the warm entry (cold-init zeros gap closed). Flagged, not tuned: at δ=0.10 warm entries A1's one-pass delivered accuracy ≈ 0.24 vs A0 ≈ 5e-9 — the declared F=10 check will engage its declared fallback at N=25; interpretation rule (R9 capability-difference clause) written into the plan pre-campaign. The agent also caught phantom values in an interrupted notification and re-anchored every number to the artifact chain (clean 04f0fd49). Report: `deprecated/A36_phase_a_campaign.md` |
| **A37** | **process-line-vs-runtime-order diff** — validate the extracted static process line (the sibling instrument's DFS source-order `call_index`: dedup'd per scope, `call_index=None` downstream of undecidable branches — `src/PROCESS_DSM/graph/edges.py` docstrings) against the **runtime-traced** execution order (PASS_TRACE / node census), per deck and per branch (solve vs output path), classifying every divergence: branch-dependent interleaving (M119's solve/output inversion is the known exemplar), first-evaluation-only writes, repeated-callee dedup. Minted at the user's instruction (2026-09-04, the M119/lagged-edge discussion). A35 subsequently excluded (c-order) for the named carrier — the DSM row order already has reader before writer — so the scope is **instrument validation**, not carrier hunting | A35 ✓ | **CANCELLED HERE / TRANSFERRED 2026-09-04** (user decision, same day as the mint: A35 *confirms* the DSM and its ordering rather than questioning them — the carrier edge is present in the export with reader correctly before writer — and instrument validation of the process line is the sibling study's scope, not this repo's). Task description handed to the `PROCESS_code_analysis` orchestrator session (message 2026-09-04, framed explicitly as a user-directed task transfer, not a defect report — the demonstrated-defect bar is untouched); execution, numbering and pin are theirs; this repo holds no dependency on the outcome. **Accepted same day as their M120** (process-line-runtime-validation), dispatched on their side; they log A35's FirstWall→Build confirmation as external corroboration matching their M117 static-side verdict, and will route back only findings that implicate this repo's *consumption* of their process line. **Watch resolved 2026-09-04 — M120 reported, nothing routed back**: the tokamak process line is runtime-validated (338/338 canonical solve calls in identical relative order, presence fully accounted, traced-vs-control bit-identical 8602/8602); its one HIGH defect is stellarator-only (a driver declared inside a pruned output branch — "the tokamak has no equivalent"), its `call_index` defect has zero cross-model occurrences on the tokamak, and our consumption is at model/block level either way. Their §6.3 locates the first-evaluation class entirely in value seeding (three latches, two in the tokamak tree: `pfcoil.py:605`, `physics.py:1381`) — consistent with A35/V14's once-per-run treatment. A35 cited there as read-only corroboration |
| **A38** | **audit-rerun** — the corrected Phase A similarity accounting run now, ahead of V3 (user instruction 2026-09-04, relayed verbatim by session `process-surgery-46` and confirmed by the user in the orchestrator session; = V3 improvement-list item 4 / V3 plan §3.1 arm A1u, §4.1, gate G4, harness H4, tally T-a). Re-run V2's Phase A design **unchanged** (A0 + the unseeded block arm, seeds 1–25, δ=0.10, τ=1e-6, a26 artifacts, pin from the stream on pulsed decks, W=3) under a corrected audit; **no `process/` change; V2's directory not edited, its runner machinery imported.** Scope: (1) instrument, **additive only** — `v2_eval_one.py`'s exit audit writes the per-component scaled residual vector (hex per key; `Residual.scaled`/`idx_c` already hold it) to `audit_residual.json` and computes at audit time the **restricted maximum over components NOT owned by the deck's post-solve node set** (`postsolve_<deck>.json` nodes → `node_writesets.json` per-scenario `writes_by_node` → intersected with the a26 spec keys; checked: 124/125/125 of 840/846/827 excluded, 716/721/702 remain; st's `pulse` contributes 0); V2's records and `--verify` unaffected. (2) Committed campaign-and-tally script (§15), `a38_audit_rerun.py`, stages preflight/refs/gates/campaign/tally/report, failure paths reachable. (3) **Gates with teeth**: G4 doctored-component pair (a doctored post-solve-owned component trips the whole-state audit and not the restricted one; a doctored in-loop component trips both); A36's entry-state and warm gates re-run; count-identity vs V2's own records (per-run node calls bit-identical, licensed by the `process/` tree hash being identical to the V2 campaign commits ba69c05d/6d9ff4b9 — asserted, not assumed); and — orchestrator addition at mint — the re-run's **unrestricted** per-record audit values must reproduce V2's published ones bit-for-bit before the restricted statistic is published (pins "additive" to a bit-comparison). (4) Tally: restricted similarity median+p90 vs F=10, whole-state audit beside; per-run carrier closure from `perturbation.json` vs A35's coefficients (nof, st; lad reported without a prediction); cost ratio + bracket must reproduce V2's 0.522/0.568/0.502. **Pre-declared expectation: F still fails, on the carrier term alone** (A1 restricted ≈ 5e-4 vs A0 ≈ 5e-9 on the traced decks). ≈150 seed runs + refs/gates, ~1 h at W=3, one heavy slot (confirmed free at dispatch). Report `A38_audit_rerun.md`, §16 captions, assessed before merge (§5). V3's A1u row is then measured at this commit; V3 licenses reuse or re-runs at the V3 driver commit | A35 ✓, A36 ✓, V2 ✓ | **MERGED 2026-09-04 (merge `e9e7e965`).** Executed by session `process-surgery-46` in worktree `A38-audit-rerun`; orchestrator pre-merge recheck (`a38_recheck.py`, committed) **27 checks ALL PASS**. **The corrected similarity criterion still FAILS on all three decks, on the carrier alone** — A1 restricted medians 6.4e-4 / 9.8e-4 / 1.15e-3 vs A0 5.0e-10 / 0 / 5.4e-9 (five to six orders over F = 10); every gate PASS with teeth; **identity vs V2 150/150 bit-identical** (counts, objective hex, audit hex, full exit state — the mint's added gate held as a measurement); count ratios reproduce 0.5217/0.5680/0.5016 exactly. Composition: nof 25/25 restricted argmaxes are A35's closed images (rel diff ≤ 6.1e-7); st 25/25 linear images of the pair (17 A35's, 8 `blanket.vol_shld_inboard` at measured gain 47.0, relative spread 2.3e-11); **A35's coefficients transfer to lad 25/25 at 1e-11** (the untraced deck), but lad's argmax in 21/25 is `tfcoil.m_tf_coil_superconductor`, **not one linear image of the pair** (fit residual median 0.067, max 0.92) — the one open term, owned by V3's G3c. Two aborted campaign attempts disclosed, cited nowhere. V3's A1u arm is hereby measured at this commit (reuse licensed by G1). Report: `deprecated/A38_audit_rerun.md` |
| **A39** | **v3-plan** — V3 plan §8 T1 (user start instruction 2026-09-04, minted with A40/A41/A42): write `arch_surgery/MDA_partitioning_experiment_v3/EXPERIMENT_PLAN.md` from `V3_DEVELOPMENT_PLAN.md`, restating every selection with its pre-declared acceptance rule (arms §3 incl. A1u and the prime in A1/B2/B3; checks §4 incl. floor = 1e-6 relative, multi-attractor clustering, deck-invalid-seed statistic, restricted audit with whole-state beside; gates G0–G7 with teeth; N = 25, δ = 0.10, τ = 1e-6; regime disclosure verbatim; A38-reuse clause licensed by G1); approval status header **NOT YET APPROVED** until the user's dated approval. Plus **D-a**: draft the prime's defect note for the sibling's owed M117 row (read-only draft in our repo; handoff is the orchestrator's, per the demonstrated-defect rule) and **D-b**: the `DSM_VALIDATION.md` "dead needs two qualifiers" convention amendment (user agreed 2026-09-04). Docs only; §16 captions | V3 plan committed (05153b6c) | **DISPATCHED 2026-09-04** — worktree `A39-v3-plan`, subagent |
| **A40** | **v3-prime** — V3 plan §8 T2, **the only `process/` change in V3** (driver scope, D11 not triggered): the `PROCESS_ARCH_PRIME` variant point in `process/core/caller.py` per plan §2 (env-switched, unrecognised value raises, `fw.set_fw_geometry()` at the head of `_call_models_once`, **not** routed through `_node`); stamping in the runners (`env_PROCESS_ARCH_PRIME`, `arch_prime_name`, `n_prime_calls` — stamped, never pooled); the variable added to every cleared-switch list. Gates with teeth, committed script, §15: **G1** prime-unset byte identity vs the pre-change commit (3 decks, MFILE hex, both runs fresh at their commits, stamps recorded); **G2** prime-on fixed-point map bit-identical from each deck's reference exit snapshot (flat + per_module); **G3** prime-on cold chain on nof + st (outer passes 3 → 2; in-run `exit_audit` 0 above τ, construction named; prime-off must reproduce A35's 3 passes and 244/124); **G3c** lad carrier census (A35's `trace` + `restarts` stages, prime off then on — names lad's coefficients and tests whether A38's open term `tfcoil.m_tf_coil_superconductor` closes or survives). Heavy-slot priority holder among A39–A41 | V3 plan (05153b6c); A35 ✓, A38 ✓ | **DISPATCHED 2026-09-04** — worktree `A40-v3-prime`, subagent |
| **A41** | **v3-harness** — V3 plan §8 T3: the v3 directory. **First commit = verbatim copy of the V2 harness** (`v2_config.py`, `v2_runner.py`, `phase_a.py`, `phase_b.py`, `run_experiment.py`, `v2_report_analysis.py` → v3 names, provenance in docstrings, no other change), then modifications as separate commits (user directive: diffs legible against V2): `v3_config.py` with `EXECUTION_APPROVED = False` + `INSTRUMENTATION` ledger (`prime`, `exit_forensics`); v3 runner composing arms from nothing (A1/B2/B3 add the prime); **H3** exit forensics at every exit (`n_solver_iterations`, `ifail`, ladder stage, constraint residual vector, active set — a missing field is a tally refusal, **G7** teeth); tally constructions **T-a…T-e** (restricted similarity via A38's committed machinery; declared pairing + taxonomy + deck-invalid-seed; floor + clustering + hop rates; per-block split; per-run carrier closure); **G4** teeth as A38's; `v3_report_analysis.py --verify`. Light runs only (smoke, G7, parser teeth), W = 1, heavy slot yielded to A40 | V3 plan (05153b6c); A38 ✓ | **DISPATCHED 2026-09-04** — worktree `A41-v3-harness`, subagent |
| **A42** | **v3-campaign** — V3 plan §8 T4: gates G0, G5, G6 at the V3 driver commit; Phase A (A0/A1u/A1, seeds 1–25, A38 reuse if G1 licenses); Phase B (R/B0/B1/B2/B3, N = 25, st skips B1); tallies; timing as context (3 serial reps, ranges); per-node unit-time reconstruction (item 11a); the V3 experiment report | A39, A40, A41 merged; **the user's dated execution approval flips `EXECUTION_APPROVED`** | **BLOCKED — minted 2026-09-04, not dispatched** |
### Parked — not blocking

`A9`–`A12` are the **subdriver-lift** experiment, a separate line from the MDA partition.

| # | Task | Prereqs | Status |
|---|---|---|---|
| **A6** | **characterise** — Stage 5. Scenario sweep; pulsed and steady-state reported separately; wall clock decomposed into model evaluation, VMCON overhead and I/O | A5 ✓ | QUEUED |
| **A8** | **plan-relocation** — move `MDA_PARTITION_EXPERIMENT.md` into `docs/plans/` per the plans convention, and fold the corrected speedup mechanism (feed-forward nodes drop from `S_global ×` to `1 ×`, so `\|all\|` shrinks as well as the sweep counts) into §3.2 | A1 ✓ (the running agent holds the current path) | QUEUED |
| **A9** | **subdriver-count** *(PROPOSED)* — Stage L0 of [`SUBDRIVER_LIFT_EXPERIMENT.md`](SUBDRIVER_LIFT_EXPERIMENT.md): confirm each nested root-find is on a `run()` path **by invocation counting, not by reading** (trap T1); time them as a fraction of wall clock; record non-convergence. Read-only, no refactor. Its gate decides whether the runtime claim survives | A1 ✓ | **UNBLOCKED** by D11 |
| **A10** | **subdriver-extract** *(PROPOSED)* — Stage L1: extract each residual into a named function behind an env switch, inner solve remaining the default. Gate: switch unset ⇒ bit-identical | A9 ✓ | **UNBLOCKED** by D11 |
| **A11** | **subdriver-lift-one** *(PROPOSED)* — Stage L2: lift the loosest-tolerance, highest-count residual. Primary result is Jacobian accuracy against a bounded reference; runtime secondary and may regress | A10 ✓ | **UNBLOCKED** by D11 |
| **A12** | **subdriver-failure-policy** *(PROPOSED)* — Stage L4, independent of the lift: resolve whether `disp=False` at `pfcoil.py:4909` is deliberate, given the identical call at `superconducting.py:1267` uses `disp=True`. Report as a PROCESS finding | — | **PROPOSED** — runnable without the D5 ruling |

### Optional / deferred

**E1–E5 are not authorised for execution** (user, 2026-08-31). Their register, with the reasoning
and the interference analysis, is [`ARCHITECTURE_EXPERIMENT_CANDIDATES.md`](ARCHITECTURE_EXPERIMENT_CANDIDATES.md).

| # | Task | Status |
|---|---|---|
| A14 | **converge-y** — E2 | DEFERRED |
| A15 | **dsm-sequencing** — E3. **A3 no longer folds into it** while it is deferred | DEFERRED |
| A16 | **convergence-predicate-audit** — E4, read-only | DEFERRED |
| A17 | **fixed-count-scan** — E5, read-only | DEFERRED |
| — | **experiment-v2** — from-scratch revision of the MDA partition experiment: the variant loses its outer verification loop (a ~38 % receipt, vacuous on 95.1 % of 88 930 measured calls), correctness moves to uncharged instruments, matched measured accuracy becomes the only comparison basis, and the lift gets its own arm. [`MDA_PARTITION_V2_REVISION_LIST.md`](MDA_PARTITION_V2_REVISION_LIST.md) | **PLANNED, not authorised** (user, 2026-09-03) |
| — | **sequencing-comparison** — retired to [`deprecated/SEQUENCING_COMPARISON_EXPERIMENT.md`](deprecated/SEQUENCING_COMPARISON_EXPERIMENT.md); if revived its home is the `functional_PROCESS` programme, not this one | OUT OF SCOPE |

### Merged

Kept in full: the status cell carries the finding, and several were later corrected or overturned. Read the status, not the title.

| # | Task | Prereqs | Status |
|---|---|---|---|
| **A1** | **stage0-rebaseline** — env-switched probe at `c0ae5b28`; sweep anatomy; three gates. Report archived at [`../reports/deprecated/A1_stage0_rebaseline.md`](../reports/deprecated/A1_stage0_rebaseline.md) | — | **MERGED** 2026-08-31 (`e9747707`). **All three gates PASS 4/4** under `PROCESS_surgery_env` at `n = 5`: switch-neutrality (0 differing MFILE lines across 11 arms), determinism (bit- and sweep-identical), baseline solves (`ifail = 1` everywhere) |
| **A2** | **module-convergence** — Stage 1, the gating measurement. Report [`../reports/deprecated/A2_module_convergence.md`](../reports/deprecated/A2_module_convergence.md) with the orchestrator's assessment | A1 ✓ | **MERGED** 2026-08-31 (`0c0466c5`). **GATE STOPS THE PARTITION.** No module is the laggard (`S_global ≈ S₁`; M1 joint-last in 82–85 % of loops, never strictly ahead). Predicted saving 4.8–23.2 %, of which the separable feed-forward hoist is 1.7–8.2 % and the partition's own contribution is **3.8 % and 7.2 %** on the two large pulsed tokamaks. **H2/H3 survive** (`k = 1`; `st_regression` has zero live cross-module back edges — open question 1b resolved *for* H2). **H4 refuted** |
| **A3** | **build-reorder** — Stage 2: move `build.run()` to after `PlasmaConfinementTime`. Gate: **bit-identical** | A1 ✓, A22 ✓ | **MERGED 2026-09-01.** **Gate PASS 4/4, bit-identical.** 0 differing MFILE lines and 0 differing hex floats on every scenario, over stated denominators (16 174 / 16 435 / 18 692 / 15 917 lines; 13 559 / 13 455 / 13 493 / 13 487 floats); `ifail` unchanged; sweep counts and per-node call counts equal **node by node**, not merely in total. Built as VP1/F7a — a three-node list `caller.py` walks, `PROCESS_ARCH_SEQUENCE` selecting it, resolved once at import, **upstream order the default** and an unrecognised value raising rather than silently defaulting. 43 insertions in `caller.py`; **nothing under `process/models/`**. M1 is now contiguous, so A5/F10 are unblocked. Two things the agent did that the brief did not require and that should become standard: it **proved the gate capable of failing** before accepting its zeros (a 1-ULP change to `rmajor`, relative 2.2e-16, is caught as 1 line of 16 174), and it **found and reported a defect in its own comparator** — an MFILE line parser anchoring on the first `(...)` silently dropped ~1 000 floats per scenario, understating the denominator to 12 550. Both are folded into the protocol below |
| **A7** | **repo-readme** — [`../../README.md`](../../README.md) written as the study's entry point, plus a delimited fork notice at the top of upstream's root README. Staleness headers added to every superseded document (I-3) | — | **DONE** 2026-08-31 (orchestrator, not a task branch — documentation only, no code) |
| **A13** | **feedforward-hoist** — the partition's **Stage 1b**, folded in by D12. Run the nodes that feed nothing back once after the fixed point instead of every sweep. Pure `caller.py`, `k = 0`, no dimension penalty, expected **4.6–8.2 %**. **The node set is arm-dependent** — `Pulse` joins it only once A4 lifts the coupler — so the hook must derive it at run time (framework C2a) | A1 ✓ | **MERGED 2026-09-01. All three gates PASS 4/4, each shown capable of failing first (protocol §12).** Switch-neutrality: 0 differing MFILE lines and 0 differing hex floats, denominators 16 174 / 16 435 / 18 692 / 15 917 lines and 13 559 / 13 455 / 13 493 / 13 487 floats. Correctness: **bit-identical with the hook ON** — 0 of **121 295** quantities differ, `ifail = 1` everywhere — which is a far stronger statement than the `norm_objf` + feasibility gate asked for, and the best available confirmation of the feed-forward property. **Saving 6.56 / 6.76 / 6.64 / 2.63 % of model evaluations**, with **sweeps unchanged** — the hoist removes work *within* sweeps, not sweeps. Built as VP2/F7b on A3's VP1 pattern, node set derived at run time from the committed node map so `pulse` joins automatically once A4 lifts the coupler (C2a). **The finding that outranks the saving:** the hoistable set depends on the active **figure of merit** — `objectives.py` reads `costs.coe` (FOM 6) and `costs.cdirt`/`concost` (FOM 7), so a deck using either must keep `costs` in the loop or the predicate would test state the loop has stopped updating. `large_tokamak_eval` takes the default FOM 7, which is the whole of its shortfall to 5.25 %. Constraints read **nothing** the tail writes — measured over 2 288 fields, not assumed. No `process/models/` change |
| **A18** | **experiment-framework** — **re-scoped 2026-09-01 (D13 + minimisation audit).** Build **Phase A only**: F4 (DSM node map + subset assertion), the `harvest` probe mode, and F11 (the C9 fixed-point engine, arms, runner and the τ calibration ladder). Lives entirely in `arch_surgery/`; **no new hook site in PROCESS** — `sweep(models, data)` already passes what the harvest needs. Superseded scope: the old row asked for F1–F6 + F7a, most of which the audit moved to Phase B or found unqueued | A1 ✓ | **MERGED 2026-09-01.** Built Phase A end to end: the DSM node map and subset assertion, the `harvest` probe mode, and the C9 fixed-point engine with its arms, runner and τ ladder. Ran the comparison over **600 harvested design points** across the four decks. Its harvest is the reused input for A22, A23 and every future replay, and now lives at `arch_surgery/idf_probe/runs/a18/` in the main tree rather than in a worktree. **Two defects found after merge and fixed on the branch** — `call_models` comparing a shorter constraint vector on the `fsolve` path, and a `gates` predicate that returned PASS on an empty check set — which is why protocol §5 now makes the orchestrator's assessment gate the merge |
| **A19** | **frozen-input-convergence** — replay-and-predict test of the one assumption A2's STOP rests on: do `Sᵢ` measured inside the coupled loop transfer to the partitioned case? Report [`../reports/A19_frozen_input_convergence.md`](../reports/A19_frozen_input_convergence.md) | A2 ✓ | **MERGED** 2026-08-31. **The middle band — the STOP is neither confirmed nor overturned.** They do not transfer, and A2 was biased against the partition: contribution rises from 2.3–7.2 % to **11.3–19.5 %** on the two large pulsed tokamaks. **Nothing reaches 25 %.** But the mechanism is the `k = 1` lift, not "M2 chasing a moving target" — `S₂` is *identical* per `call_models` in all 2 447 replayed loops. Netted for the lift's `2n → 2(n+1)` gradient penalty (4.8–5.0 %, absent from A2's arithmetic) it is **6.6–14.5 %**, and under the partition the laggard becomes **M2** at 42 % of measured cost. **The user decides whether the band's "proceed with the expectation revised down" rule is invoked** |
| **A20** | **registry-append** — F2. Appended iteration variable 178 and constraint 93, gated, then **withdrew the code by user ruling**: synthetic content does not earn a permanent place in a frozen tree. The deliverable was the knowledge, and it stands | A1 ✓ | **MERGED 2026-09-01** — gates PASS 4/4; `process/` back to probe-only; A4 takes 178/93 |
| **A21** | **partition-report** — **SCOPE EXTENDED to cover Phase B as well as Phase A** (user, 2026-09-01): one runnable file and one academic reference document spanning both phases. **RUN PRE-APPROVED by the user, 2026-09-01**: no further approval is needed before executing the experiment via the wrapper script. Scope of that approval, as understood: fresh isolated subprocesses, artifacts to the untracked `runs/`, **no writes to `process/`, no push**. If the wrapper's counts differ from A18's, that is a **finding to report, not a discrepancy to reconcile quietly** — the two invocation paths agreeing is itself a determinism check. — deliver the experiment as **one runnable file plus one academic reference document**, for a reader who has not followed the queue. (a) `MDA_partition_experiment.py`: a single clean entry point over A18's gated `arch_surgery/fixedpoint/` package, concise jargon-free docstring, runs the whole of Phase A end to end. (b) `MDA_partition_exp_results.md`: goal, hypothesis, method (culminating in *run this one file*), results, critical discussion, conclusion — then a **second pass criticising the method and results, with the findings folded back into the discussion**. **Results must come from an actual run; nothing is written ahead of the data** | **A18** | **MERGED 2026-09-01. Phase A is written up.** Two deliverables: `MDA_partition_experiment.py` (one entry point wrapping the existing gated runners — a consolidation, not a reimplementation) and the standing `MDA_partition_exp_results.md`. **Scope was Phase A only** (orchestrator's dispatch brief, 2026-09-01): written from the recorded artifacts with **no re-run**, because A24 held the one heavy slot; the earlier 'SCOPE EXTENDED to cover Phase B' and 'results must come from an actual run' wording in this row was superseded by that brief and is corrected here. Determinism smoke check passed — the wrapper's collated output is identical to the same analysis invoked directly, and no count differed from A18's. **It also produced three substantive corrections to A18's merged report**, all re-derived independently by the orchestrator before merge and now carried as a correction note on A18 itself. **The outstanding commitment to `PROCESS_code_analysis` is discharged, not owed** — the `objf`/constraint magnitude distribution is in §4.6 in full. Phase B is left as a stated gap for A25 |
| **A22** | **outer-pass-census** — **the cheap query that can stop Phase B before any code is written.** For each scenario *separately*: when the block arm needs a second or later outer pass, **which coupling-state fields are still moving, and which module wrote each?** If the movement crosses a module boundary on a *pulsed* deck, then `k = 1` is wrong for that deck, lifting the burn time will not decouple the modules, and Phase B's premise fails there. Also, on its own terms: `st_regression` has zero live cross-module back edges (A2) yet its outer loop needs 2.139 passes — unexplained *(now explained: see the status column)* | A18 ✓ | **DONE / MERGED 2026-09-01** — **the premise holds on all four decks.** Cross-module movement on the pulsed decks is the burn time and nothing else: the pin arm removes **every** above-tau field on outer pass ≥ 2, on 149/149, 297/297 and 10/10 points, and M2 never re-solves at all. `st_regression` has **no** cross-module movement of any kind — A2 missed nothing; its 2.139 passes are one structural verification pass (128/144) plus a numerically degenerate 1990-cost evaluation on 7/144 points, which A18's own `hoist = 1` variant removes exactly (→ 1.9375). Gate: 600/600 design points reproduce A18 bit-for-bit, no tolerance anywhere; **no `process/` change**. Report archived; loop-side saving and its limiting arithmetic recorded in the plan's §4.1a |
| **A23** | **flat-arm-permutation** — **closes a confound in Phase A's headline comparison that neither A18 nor A3 could see alone.** `arms.build_blocks` groups nodes by module and so runs `plasma_geom, physics, build, …`; `arms.loop_nodes` keeps the harvested upstream order and so runs `plasma_geom, build, physics, …`. **A18's A0 → A1 therefore varied the module grouping and a sequence permutation together.** A3 shows that permutation is bit- and sweep-count-identical in the incumbent driver R, which makes it very likely inert in the flat arm too — but *very likely* is not a measurement, and block-vs-flat is a headline. **The check:** re-run arm A0 over A18's existing harvested design points with `loop_nodes` permuted to the block order, and compare counts against A18's recorded A0 **per design point, bit-for-bit**. No new PROCESS run, no new harvest, no `process/` change. If the counts are identical the confound is retired and A0 → A1 is purely the grouping; if they are not, then **the permutation is part of what Phase A measured and every block-vs-flat number needs restating with that said** | A18 ✓, A3 ✓ | **MERGED 2026-09-01. The confound is retired: `A0 → A1` is the module grouping alone.** Arm A0 replayed in the block arm's order reproduces A18's recorded A0 **bit-for-bit on 600 of 600 design points**, zero skips — counts, converged flag, moved-constant list, the residual trace **at every sweep**, and the exit audit. Extended beyond the brief to every setting A18 recorded A0 under (hoist off and on, tolerance ladder 1e-4 / 1e-6 / 1e-8): **2 400 / 2 400 identical**. The permutation is 2 of 21 positions on all four decks — `physics` and `build` transposing, i.e. **A3's reorder reached independently by grouping**. **The harvest-reuse licence had to be rebuilt** and A22's tree-hash test no longer applies: `caller.py` has changed twice. The replacement argument, verified by the orchestrator from the artifacts — `process/models/`, `process/core/solver/` and `process/data_structure/` tree-hash identical to each of the three harvest commits, and `Caller.call_models` / `_call_models_once` / `_node` entered **0 times** across the whole replay, counted rather than assumed. This also means **A3's and A13's gates do not license the reuse**, because they gate a path the replay never enters. Gate teeth: one ULP moves 488/600, a reversed-order control moves 575/600. **Scope, binding on A21:** this licenses *one transposition of two adjacent nodes is inert*, never *node order does not matter* |
| **A24** | **phase-b-scaffold** — **F2 + F9 + F6 in one task.** F2: append iteration variable 178 and constraint 93, with the `lablcc` extension D14(a) approves. F9: extract the burn-time residual in `process/models/pulse.py` so its *solution method* becomes a driver choice (the VP5 pattern, framework §2.5), D11/D14(b)-approved, **expression unchanged**. F6: the correctness and robustness gate harness the variant will be measured with. **All three are inert** — nothing selects them — which is why they share one gate | A20 ✓, A3 ✓, A13 ✓ | **MERGED 2026-09-01. Bundle gate PASSES — the bundling premise held.** 0 of **121 295** quantities differ against `7a0f3f6e` on all four decks, no tolerance anywhere; `ifail` unchanged; sweeps unchanged; `tests/unit` 843 passed. All three pieces are inert, as D16 assumed. The §12 sensitivity check **again found a defect in the agent's own harness** (an `ifail` census keying on `str()` while the parser returns a float, reporting `n_ifail_1 = 0` over four runs that all had `ifail = 1`) — third task in a row. **Premise correction, now in the plan:** the burn-time site is **not** an inner root-find; `Pulse.run` assigns a closed-form expression, so lifting removes no inner-solve work and A25 must not claim it does. A9–A11's sites *are* genuine root-finds, so that expectation does not transfer either way |
| **A25** | **phase-b-variant** — **A4 + A5 in one task, plus the equivalence gate and the H5 run.** A4 lifts the burn time onto the optimiser using A24's registry and VP5 seam; A5/F10 adds the per-module solvers A3 made possible by giving M1 a contiguous span. **A19 established these are not separable** — `max Sᵢ` is unchanged when the coupler is pinned, so lifting buys nothing without the module solves and must be measured with them — so bundling them follows the evidence rather than cutting a corner. Includes D15's δ calibration and, **only if the equivalence gate passes**, the H5 multi-start run | A24 | **MERGED 2026-09-01. Equivalence gate PASSES 4/4, H5 ran, and the proposed architecture DOES NOT WIN.** Per deck, 25 paired starts at δ = 10 %, cost in model node calls: `large_tokamak_nof` **loses narrowly, +2.0 %** with robustness identical; `low_aspect_ratio_DEMO` **inconclusive** (8 starts, ratios 0.24–3.26); `st_regression` **−5.1 % on cost but loses 2 of 25 on robustness**; `large_tokamak_eval` **+16.8 %, loses 10 of 25**. The variant never solves a start the baseline cannot, and **robustness outranks cost**, so by the outcome table declared in advance **H5 fails**. §4.1a's ≈ −26 % is **not** confirmed: against the incumbent the loop-side saving is −2.6 % / −5.9 %, an order of magnitude below A22's −29 %, for exactly the reason §4.1a's own condition 1 named. Switch neutrality 0 of 121 295; `tests/unit` 843 passed; no `process/models/` change beyond A24's. **The gate caught three defects in the agent's own work first — fourth consecutive task.** The serious one: `icc = 93` appended at the end of the deck became the 24th **inequality**, because PROCESS decides equality membership by **position** in `icc`; that variant returned `ifail = 1`, a plausible objective, and looked **38 % cheaper**. It would have been this task's false headline. **Orchestrator overturned the report's caveat 1** — see the correction header on the archived report: the NaN loophole is real but did not operate here, the robustness deficit is genuine, and the negative verdict is better supported than the report claims |
| **A26** | **method-fixes** — **fix the methodology before Phase B is re-run** (user, 2026-09-01), acting on the results report's own §6. Seven changes: **(1)** the arms compared at **matched final accuracy** via cost-versus-achieved-accuracy curves, not matched tolerance — §6.1's objection, and the one change that could overturn Phase A's headline; **(2)** one settled accounting, the driver-side figure canonical and the engine a stated cross-check; **(3)** no exclusion on constancy — every component tested, near-zero scales given a recorded floor, exclusions only where accumulation is justified; **(4)** a manifest per comparison declaring what varies, asserted at run time, plus the figure-of-merit guard for `hoisted_nodes()` (closes **I-13**); **(5)** timings with an uncertainty band, as context only; **(6)** `large_tokamak_eval` **dropped**; **(7)** the PROCESS-defect handoff is A27's, not this task's | A25 ✓ | **MERGED 2026-09-02. It overturns Phase A's headline.** At matched **achieved** accuracy the partition costs **−4.3 % / −4.5 % / −13.1 %**, not the published **+46.8 % / +40.4 % / −4.6 %**. §6.1's "at most" bound was doing all the work and its counter-argument is refuted. **The replacement claim is narrow and must stay narrow:** this removes the finding that the partition *costs*; it does not show it is worth anything. At its cheapest matched setting the block arm runs **1 172 of 1 248 inner solves in one sweep** — barely blocking — and the comparison rests on **p90**, the median exit residual being exactly 0 on 15 of 17 rungs, with the arms indistinguishable on the worst point (0.82–1.23). **The agent's own first analysis gave +21.9 % and it changed the sign itself**, recording the envelope choice as AD2 with its reversal rather than burying it. **Fix 3 costs nothing** — testing all 840/846/827 components at three floors a decade apart changes **no count**, 0 of 590 points invalidated, so the Phase A numbers do *not* move from that quarter. **`pulse` out of the MDA is worth 3.35 % / 3.31 %** net of tail, against A25's ~1.5 %; and **plan §4.1d's gate as I specified it was vacuous** (no deck activates `icc = 41`, so `conf` cannot move) — replaced with the field itself as hex, 0 of 1 710 differing. **Timings resolve nothing and say so:** p10–p90 spread is **50–143 %** of the median against 4 % effects, which is I-10 reproducing itself. **Two corrections to published claims**, both verified by the orchestrator: three constants moved, not one; and §6.3's harvest-reuse licence no longer holds since A25 changed `pulse.py` and `numerics.py`. **A0′ is nearly the degenerate single-block `module_solve`** — two small blockers named for A28 |
| **A27** | **process-defect-handoff** — §6.5 item 5: route the findings that are about **PROCESS itself** rather than about architecture to the sibling `PROCESS_code_analysis` study, with denominators (user, 2026-09-01). Four defects: the stopping test reporting a **NaN state as converged**; `np.allclose`'s hidden `atol=1e-8` (18.0 % of nonzero MFILE quantities below the crossover, 203 unconditional; 19.0 % of one deck's constraint entries); the **1990 cost model diverging at negative net electric power** (7 of 144 points); and **constraint equality membership decided by position in `icc`**, which gave A25 a plausible, `ifail = 1`, **38 %-cheaper false result** until a gate caught it. Documentation only | A25 ✓ | **MERGED 2026-09-02.** One outgoing report superseding the 2026-09-01 one, **five** defects rather than the four briefed. New: two `init.py` loop bounds that are not the constraint count. It also found the **mechanism** behind the cost divergence — `costs.py:2751` clamps `max(kwhpy, 1e-10)`, a guard against `kwhpy = 0` applied to a *negative* `kwhpy`, so the division becomes ×10¹⁹ and the arithmetic closes exactly. **It corrected four of our own records, two verified by the orchestrator:** the `10¹⁸ times tighter` is **ours, not upstream's** (upstream's tolerance at `coe = 6.6e21` is *looser* than ours by 5.3 × 10¹⁸ — I-12 amended); and a NaN **cannot** reach `check_agreement` through the constraint vector because `constraints.py:2035` raises first, so the superseded report's blast-radius argument was too broad. It also found three wrong line numbers in that report — **partly right and partly wrong, which suggests it was read off a working tree rather than the base commit**, a process failure worth remembering. Carries an explicit section on what was **not** established, including that **no defect has been shown to have changed a published PROCESS result** — for the NaN case that is a measured refutation, for the rest simply unexamined |
| **A4** | **burn-time-lift** — Stage 3. **Not separable** (A19): `max Sᵢ` is unchanged when the coupler is pinned, so it buys nothing without A5 and must be measured with it. Report the `2n → 2(n+1)` penalty separately, **with its denominator stated** — the two ranges in circulation are the same quantity counted two ways. `nvars` is **20 / 19 / 14 / 2** (`large_tokamak_nof` / `low_aspect_ratio_DEMO` / `st_regression` / `large_tokamak_eval`, counted from each deck's `ixc` entries). As a fraction of the **current** cost the lift adds `1/n` = **5.0 % / 5.3 %**; as a fraction of the **new** cost it is `1/(n+1)` = **4.8 % / 5.0 %**, which is where A19's range came from. Plan §4.1a composes it against A22's loop-side saving on the current-cost denominator; use that one, or say which you used | A3 ✓, A13 ✓ | **SUPERSEDED — delivered by A25**, which implemented A4 and A5 as one task under D16. Reinstated by D12 as a separate row; that reinstatement is spent. Kept for its prerequisite trail and for A19's non-separability finding, which is why the bundling was correct |
| **A5** | **module-solvers** — Stage 4. Carries A19's warning: the global loop exits with M2's state still moving in 24 % of calls, so per-module criteria will converge state the loop currently skips — **more work, not less**. Measure that before any wall-clock comparison | A4 ✓ | **SUPERSEDED — delivered by A25** in the same bundle as A4 (D16). Its warning was measured: read A25's and A28's results rather than this row |

### User-facing standing items (not tasks)

- ~~Add `upstream` read-only for drift measurement~~ — **DONE**, verified
  (`upstream https://github.com/ukaea/PROCESS.git`).
- ~~Push `architecture_surgery`~~ — **DONE**, verified (0 commits ahead of origin).
- Delete `github.com/wrutten/PROCESS_surgery` — reported done by the user; **not verifiable from
  this session** (no network access to GitHub).
- **One heavy slot at a time on our side** (user, 2026-09-01). The machine is shared with
  `PROCESS_code_analysis`, and the user allocates **one concurrent measurement slot to each
  study**. So: **at most one task running PROCESS solves at a time**, however many light tasks
  (replays, analysis, documentation) run alongside it. Parallel dispatch is allowed by the
  protocol and is still the right default — the constraint is on *heavy* slots, not on agent
  count. **Classify before dispatching, and when unsure treat it as heavy**: a replay over
  hundreds of harvested design points is real CPU even though it runs no solve. This rule was
  written because the orchestrator dispatched A13 (heavy) and A23 (a 600-point replay)
  concurrently, and had to stop A23 seconds in.
- **Run measurement work when the machine is otherwise idle** (user, 2026-08-31). With 7 GB RAM and
  no thread pinning, concurrent sessions are the leading suspect for I-8's spread.


---

## Known open questions (parked, not blocking)

1. ~~Which module is the laggard?~~ **Answered, then revised.** Under the coupled loop none is
   (A2): `S_global ≈ S₁ ≈ S₂ ≈ S₃`, M1 joint-last in 82–85 % of loops. **A19 revises it**: with the
   lift applied the laggard becomes **M2**, at 42 % of measured cost — so §3.2's requirement that a
   *small* module drive the loop is still unmet.
1b. ~~Does H2 survive its own cheapest test?~~ **Yes.** `st_regression` has zero live cross-module
   back edges and is the most independent of the four scenarios; A1's 39.7 % was not diagnostic.
1c. ~~Should A13 be revived?~~ **Yes — D12 folds it into the partition as Stage 1b**, taken first
   because it is the only separable component.
1d. ~~Is the 10–25 % band enough to revive A4/A5?~~ **Yes — D12.** The considerations below stand
   as the risks the revived experiment carries, not as arguments against it. Original text:
1d-orig. **Is the 10–25 % band enough to revive A4/A5?** A19 moved the partition's contribution into
   the band on three of four scenarios (11.3–19.5 %), so the plan's "proceed with the expectation
   revised down and stated" rule is now live where before the "< 10 % → stop" rule applied. What
   argues against reviving: netted for the lift's dimension penalty the two large pulsed tokamaks
   fall to 6.6–14.5 %; H5 (the consistency constraint changing VMCON's iteration count) is still
   unmeasured and the plan calls it the likeliest failure; the laggard is M2 at 42 % of cost; and
   per-module solver overhead, extra objective evaluations and the consistency residual are in
   nobody's arithmetic. **A19 was explicitly told this is the user's call and did not make it.**
2. ~~Post-lift, does `Pulse` join a module or remain feed-forward?~~ **Answered** — pure
   feed-forward, verified from its two state writes (partition plan §2.3a).
3. Rows 52–55 (`Objective`, `Constraints`) are downstream of everything — can the objective be
   evaluated without re-running M3?
4. How many upstream commits now separate `c0ae5b28` from `ukaea/main`, and must the write-up
   state that drift?

---

## Change log

| Date | Entry |
|---|---|
| 2026-08-31 | Queue opened. Repository flattened into the `wrutten/PROCESS` fork (D1); base commit fixed at `c0ae5b28` (D2); `710a75c9` evidence discarded (D4). Experiment plan written and revised after the DSM module decomposition (D8) and the withdrawal of the `b_plasma_vertical_required` finding (I-4). A1 (stage0-rebaseline) dispatched. |
| 2026-08-31 | Subdriver-lift experiment planned ([`SUBDRIVER_LIFT_EXPERIMENT.md`](SUBDRIVER_LIFT_EXPERIMENT.md)); A9–A12 proposed, A9–A11 blocked on a D5 ruling. Superseded IDF plan moved to `reports/deprecated/`, architecture evaluation to `reports/`. **A1 (stage0-rebaseline) was dispatched before this protocol existed** — its original brief said to commit to `architecture_surgery` and to write its report to `idf_probe/STAGE0.md`; it was corrected in flight to branch `A1-stage0-rebaseline` and to report at `reports/A1_stage0_rebaseline.md`, and given the hard rules (sandbox never overridden, no sibling-clone writes, no `git add -A`). |
| 2026-08-31 | Subdriver-lift experiment **reframed around robustness** (primary), gradient quality secondary, performance penalty measured and reported as two separate numbers rather than netted; its Stage L0 gate moved from wall-clock share to failure incidence. Portfolio of further candidates added ([`ARCHITECTURE_EXPERIMENT_CANDIDATES.md`](ARCHITECTURE_EXPERIMENT_CANDIDATES.md)) with A13–A17 proposed — E1/E4/E5 need no D5 ruling. Interference between the two planned experiments analysed: **optimiser-registry ranges now reserved above**, and §2.4 records that the partition's outcome changes what the subdriver experiment *is* (a lifted residual gains a second possible host — the module's own solver — which costs no dimension), so the partition resolves first. |
| 2026-08-31 | **A1 (stage0-rebaseline) complete, not merged.** Gates (a) switch-neutrality and (b) determinism PASS — (a) verified more strongly than specified, against a pristine `git archive` arm on hex float literals plus whole-MFILE identity. Gate (c) **FAILS** on `st_regression` (I-5), a stale input that also fails on the pristine base commit; the agent held the gate at FAIL rather than adopting a working diagnostic input, which is the behaviour protocol §6 asks for. Frozen pending the scenario ruling (open question 0b). New issues **I-5** (stale scenario), **I-6** (shared-tree branch mishap, closed by amending §3/§9 to isolated worktrees), **I-7** (no free iteration-variable number — blocks every lifting task), **I-8** (7.9 % wall-clock noise versus gates set at 10–25 %). Registry reservation table replaced with A1's measured state. Sweep anatomy: mean 3.2–3.5 sweeps per `call_models`, 38–42 % of sweeps above the 2-sweep floor, and **94–96 % of all sweeps are finite-difference gradient perturbations**. |
| 2026-08-31 | **D9** ruled: patch `st_regression.IN.DAT` in place rather than re-pointing the deck at upstream's regression inputs, keeping the scenario set a frozen artifact of this study. A1 (stage0-rebaseline) re-dispatched to apply the patch and re-run all three gates on its own branch, per protocol §6 — one merge, re-gated, no deferred fix. Isolated-worktree convention (§3, §9) takes effect from A2 onward; A1's fix continues in the main checkout by sequential handoff, with no concurrent git access. |
| 2026-08-31 | **A1 (stage0-rebaseline) MERGED** (`e9747707`) — all three gates PASS 4/4 under `PROCESS_surgery_env` at `n = 5`; patching an input disturbed neither switch-neutrality nor determinism, which was the point of re-gating the whole task. **I-1 and I-5 CLOSED**; **I-9 filed and closed** (the scenario deck had never been under version control — the root `.gitignore`'s `*.DAT` swallowed it, so Stage 0 was not reproducible by anyone else and D9's "frozen artifact" premise was false as written). **I-8 revised upward**: worst within-arm wall-clock spread is 19.6 % at `n = 5`, so a 10 % effect is not measurable here and the partition plan's stop rule sits inside the noise. **I-7 downgraded** — the cap is derived, so appending is a one-entry change (D10, allocation table added). **Open question 1b filed**: `st_regression`, the free control for H2, does not behave as the plan predicts. E1–E5 moved to Optional / deferred; the sequencing comparison retired out of scope. Framework plan added, proposed as **A18**. |
| 2026-08-31 | **A19 (frozen-input-convergence) COMPLETE, awaiting merge** — **the middle band.** A2's `Sᵢ` do **not** transfer to the partitioned case and A2's estimate was biased against the partition: replaying 2 447 of 2 451 `call_models` with each module iterated in isolation on frozen inputs puts the partition's own contribution at **11.3–19.5 %** (A2: 2.3–15.8 %). **Nothing reaches 25 %**, so the STOP is not overturned; three of four scenarios sit in the plan's 10–25 % band, filed as **open question 1d for the user**. **The task's own directional argument is refuted:** `S₂` measured with M1 solved to convergence first is identical, per `call_models`, in all 2 447 loops and all four scenarios — M2 is not chasing a moving target. `S₁`'s fall is *entirely* the `k = 1` lift (`S₁` alone equals `S₁` with `times.t_plant_pulse_burn` pinned in 100 % of loops); only `S₃` shows a frozen-input effect. New findings: **the laggard moves from M1 to M2** under partitioning (42 % of measured cost, so plan §3.2's condition still fails); **the lift buys nothing without the partition** (`max Sᵢ` unchanged to four decimals when the coupler is pinned), so unlike the hoist it is not separable and is correctly credited to the partition; **the lift's `2n → 2(n+1)` gradient penalty is 4.8–5.0 %** and absent from A2's arithmetic, netting the two large pulsed tokamaks to 6.6–14.5 %; **A2's optimistic censoring column is never attained** (true `S₂` exceeds `S_global` by +1 or +2 on every censored loop), so its reversal path "quote only the optimistic column" must not be taken; and **A2's measured-cost weighting does not reproduce** — the feed-forward share moved from 6.4 % to 4.4 % on identical code, which is **I-10** reaching the gate arithmetic, so node counts (which reproduce A2 to the decimal) should lead. Gate N19 (neutrality) PASS 4/4, 0 differing MFILE lines; method control exact, 7 058/7 058; every module ran in isolation without raising. A2's report and the partition plan annotated in place, refuted claims marked rather than rewritten. |
| 2026-08-31 | **A2 (module-convergence) MERGED** (`0c0466c5`) — **the Stage-1 gate stops the MDA partition experiment.** No module is the laggard; the partition's own contribution is 3.8–7.2 % on the two large pulsed tokamaks against a 25 % gate, and most of the total saving belongs to the separable feed-forward hoist. **H2 and H3 survive** — `k = 1` confirmed, `st_regression` has zero live cross-module back edges, so open question 1b resolves *for* the hypothesis and A1's 39.7 % was not diagnostic. **H4 refuted.** A4 and A5 **withdrawn** (not deferred — the measurement exists); A3 stays queued as a dependency-graph integrity check; **A13 returned to the user as open question 1c**, being the only remaining positive-return item. New: **I-10** (identical work varies 35 % in CPU-seconds, cause unknown — supersedes the contention reading of I-8), traps **T7** (ten models call `run()` from `output()`) and **T8** (`pkill` is inert across sandboxed Bash calls), and protocol §9 (`TaskStop`, not `pkill`). Third instance of T1 found and withdrawn — it was the orchestrator's own citation in the partition plan. |
| 2026-08-31 | Measurement method of A2 reassessed at the user's request (addendum appended to its report). The STOP is not reversed, but `Sᵢ` measured inside the coupled loop is biased against the partition, and on `low_aspect_ratio_DEMO` the gate boundary sits inside that uncertainty. **A19 (frozen-input-convergence) dispatched** to settle it by replay rather than by refactor. |
| 2026-08-31 | **D12: the partition experiment proceeds despite the gate**, under its own 10–25 % rule, with the revised expectation written out in plan §4 (hoist 4.6–8.2 %; partition beyond it 11.3–19.5 % gross, 6.6–14.5 % netted for the lift's dimension penalty; ≈11–22 % combined) and the three measured facts that cut against it stated rather than netted away — M2 becomes the laggard at 42 % of cost, the lift is not separable, and the loop exits with state still moving in 24 % of calls. **The feed-forward hoist is folded in as Stage 1b** (A13, out of the deferred register), taken first as the only separable component. A4 and A5 reinstated. Framework gains **C2a**: the feed-forward node set is *arm-dependent* — `Pulse` joins it only once the coupler is lifted — so `in_loop` must resolve it at run time; a hard-coded list would be wrong only when VP2 and VP5 compose. |
| 2026-09-01 | **D13: the partition is re-factored into a fixed-point phase and a VMCON phase**, at the user's proposal, and critically assessed. Phase A is **block vs flat Gauss-Seidel** converged on all coupling variables — which removes H5 by construction, makes the primary metric a count rather than a wall clock, and lets VP4 be tested without VP5. Three amendments recorded with it: the control must be **re-baselined under the strict predicate** (so this is VP3 + VP4, not VP4 alone, and **A2's and A19's `Sᵢ` do not transfer**); Phase A tests block-vs-flat, **not the lift**, which has no host without an optimiser; and the tight inner tolerance is **conservative against the partition** (over-solving an inner block against a stale coupler is the known-inefficient regime), so a negative Phase A result is an upper bound on the loss and must be reported as such. Parameters settled by the user: tight inner tolerance, hard cut 20. **Runtime measured, not estimated**: `large_tokamak_nof` warm is 15.9 s internal for 2 027 sweeps ⇒ **7.8 ms per model sequence**; a full four-scenario two-arm replay is **4–14 min** depending on where the strict predicate lands, so the 5-minute budget holds only with a **cached harvest pass and a 1-in-5 subsample of gradient points** (licensed by A19 §5.2 — perturbed points behave like unperturbed). **I-10's missing datum obtained as a by-product**: our peak RSS is **423 MB**, against the sibling's heaviest 0.47 GB — comparable, so memory pressure is not a plausible mechanism for the 35 % CPU spread. |
| 2026-09-01 | **Both live plans rewritten** to the five-section template the user set — problem context, proposed approach, implementation impact, expected results, critical assessment. [`MDA_PARTITION_EXPERIMENT.md`](MDA_PARTITION_EXPERIMENT.md) is restructured around D13's two phases and reframed as an **existence proof** (user, 2026-09-01): the deliverable is one fair case where a simple architectural change measurably moves cost, not a verdict on adopting this partition — so the gates become fairness gates and a negative block-vs-flat result no longer sinks the experiment. [`EXPERIMENT_FRAMEWORK.md`](EXPERIMENT_FRAMEWORK.md) splits into a **Phase A half** (lives entirely in `arch_surgery/`, touches no PROCESS code, needs no D11 approval, neutrality trivial) and a **Phase B half**; new **C9 fixed-point engine** and **C10 DSM validation reporting**; **F8/VP3 un-deferred**; **F3 timing protocol demoted** off the critical path now that counts are primary. **New finding recorded**: today's two-sweep floor is an *artefact of converging on functionals that do not exist at entry* — a proper fixed-point iteration has a floor of **1**, because the entry state is `y0`. On `large_tokamak_nof` the information-free second sweep is **630 of 2 027 sweeps, 31.1 %**, which bounds the effect from above. Also recorded: `np.allclose(equal_nan=True)` means **a NaN state is reported converged**; not reproduced in the new engine. Parameters settled with the user: `y` = all state written by in-loop models with the DSM feedback set as a parallel cross-check, `rtol = 1e-6` per component with a measured-and-justified exclusion list for accumulators, plain Gauss-Seidel (no acceleration), caps 20 inner / 20 outer / **200 module-sweeps per design point**, hoist applied to both arms in first results, pairwise drop, and a **design point defined as the pair `(x, y0)`**. Protocol gains **§11**: DSM findings go to a standing, never-archived register ([`../reports/DSM_VALIDATION.md`](../reports/DSM_VALIDATION.md)), opened with five entries — trap T1 has been rediscovered three times because each finding was buried in whichever task report found it. |
| 2026-09-01 | **V6 filed — the DSM is configuration-specific, and this had not been recorded anywhere.** It resolves conditionals against the analysis tool's `tokamak` preset, built from `examples/data/large_tokamak_IN.DAT`. Switch-by-switch: **exact match** for `large_tokamak_nof` and `large_tokamak_eval`, **5 switches out** for `low_aspect_ratio_DEMO`, **12 out** for `st_regression` (`i_pulsed_plant`, `itart`, `i_single_null`, `i_plasma_current`, the TF path). So the module decomposition is authoritative for the two large tokamaks and an **extrapolation for `st_regression`** — which the partition plan had just named its most promising scenario. Phase A's predicate is unaffected (the coupling set is instrumented, not DSM-derived); the **block arm's module boundaries** are what depend on it. Fix requested of `PROCESS_code_analysis`: regenerate the collapsed DSM per scenario, which `ProcessConfig.from_scenario` already supports. **F8 of the architecture evaluation extended**: the two-sweep floor is a *consequence of F3*, not an independent finding — `objf`/`conf` do not exist at entry, so the first sweep is information-free; a proper fixed point has floor **1**. Fixing F3 removes F3, F8 and F12 together, with no model changes and no new design variables, which makes it the highest-value single change in that document. **NaN bug report staged** at `reports/outgoing/` rather than written into `PROCESS_code_analysis` (hard rule: never modify a sibling clone) — `check_agreement`'s `equal_nan=True` reports a stably-NaN state as converged; latent, never observed firing, one-keyword fix. **Probe footprint audited** at the user's question: `process/` carries +1 632 lines since `c0ae5b28`, but every edit to an existing file is additive — 3 imports and 12 `if ENABLED:` guards, **no logic line changed** — and A1 proved switch-neutrality against a pristine `git archive`. A fresh branch is **not** recommended; F1 consolidates the three accreted probe modules instead. **Phase A needs no new hook site**: `sweep(models, data)` already receives everything the harvest needs. Partition plan gains **§4.4 robustness**, expected to change in both directions. |
| 2026-09-01 | **Framework cut back to a minimum after an over-building audit** (user: *"the framework should be light/minimal"*). New governing principle at the top of the document: build a component when a **queued** task needs it, never because a variant point exists in the model. **Phase A needs three things, not twelve** — one new probe mode (`harvest`), the C9 fixed-point engine, and the C8 node map. Cut or deferred: the `PROCESS_ARCH` arm-composition parser (Phase A selects arms in the harness, not in PROCESS — the existing single-mode switch suffices); **C3/F9, the VP5 pattern — the largest single piece, and no queued task consumes it**; VP3/VP4/`note_subsolve` production hooks (needed only if Phase A's findings are carried into the running code, which is undecided); C6 shrinks to nothing for Phase A (matched-accuracy is the engine's exit audit, not a gate; correctness and robustness need a solve); C7 shrinks to metadata fields plus the overlapping-interval rule. **F1 (probe consolidation) approved** by the user conditional on bit-identical output and negligible runtime cost, but **scoped as a pure file-merge and moved to *after* Phase A** — the harvest mode does not require it, and refactoring three working instruments on the critical path buys nothing. **C8 simplified**: the node map is stable (same pin; VP1 reorders the sequence but not the node-to-module mapping), so run-time validation reduces to a three-line assertion — and per V6 it must be **`observed ⊆ mapped`, not equality**, because the map is config-specific and some mapped nodes never execute in a given deck. **Arm A0f added and built up front** (user): strict predicate with the floor kept at 2, so `R→A0f` isolates the predicate's cost and `A0f→A0` the floor removal, which `R→A0` can only measure as a sum. Partition open questions 1 and 2 marked tracked, 3 not relevant at this time, 4 answered. **Requests relayed to `PROCESS_code_analysis`**: `run_all_scenarios` entry point for per-scenario collapsed unsequenced DSMs (registered there as M100, awaiting their user), and the NaN bug report for filing. **They verified both open items and found a second route**: `MDA_Output` has no predicate of its own — `caller.py:205` calls the same `check_agreement`, so **both definitions of "converged" share one `equal_nan=True`** — and `caller.py:204`'s `mfile_data.get(var, np.nan)` defaults a *missing* variable to NaN, so a variable that was NaN and then disappears also compares equal. **F12 escalated** in the architecture evaluation: it was rated Low on the reasoning that the definitions merely differ; they differ in what they compare while sharing the comparison function, and `MDA_Output` is the loop whose result reaches the user. |
| 2026-09-01 | **NaN bug report FILED** by `PROCESS_code_analysis` at their `docs/bug_reports/2026-09-01_call_models_equal_nan_converged.md`, at our updated revision (`a2afe039`), with a filing addendum carrying provenance, the pin's line numbers (~20 higher than ours) and their cross-references. **A footnote of theirs then broke F1 of our architecture evaluation.** `np.allclose(a, b, rtol=1e-6)` tests `|a-b| <= atol + rtol*|b|` with numpy's **default `atol = 1e-8`**, so the absolute term dominates below `|b| = 1e-2` — and below that crossover the test is *looser* in relative terms, not tighter. Measured on `large_tokamak_nof`'s MFILE (the exact set `MDA_Output` compares): **18.0 % of nonzero quantities fall below the crossover**, and **203 are below 1e-8, where agreement is unconditional — any change passes**. At `|x| = 1e-4` the effective relative tolerance is 1.0e-4, two orders of magnitude looser than F1's table states. **F1 rated the MDA agreement tolerance as the single number 1e-6; that is wrong for a fifth of the state**, and it makes the tolerance-inversion argument worse — `epsvmc = 1e-7` is asked to resolve quantities whose reproducibility floor is locally 1e-2 relative. Addendum added, crediting the sibling study. **Consequence for our engine, caught before it was built**: C9's `rtol = 1e-6` was described as "mirrors today's value" — struck from both plans. `atol_i` is now **chosen deliberately per component and recorded**, because both defaults are unsafe: numpy's 1e-8 silently reproduces the hole, and 0 makes near-zero components unconvergeable. |
| 2026-09-01 | **Cross-project thread closed; nothing owed either way except one measurement.** The sibling study linked our stable path from the filed bug report and wrote our atol quantification into their Owed row with credit, carrying the scope caveat verbatim. **Outstanding commitment, now recorded in the engine spec**: Phase A's harvest must emit the **magnitude distribution of `objf` and the constraint vector** — the idempotence loop's own set, which the F1 addendum explicitly left unmeasured because it only had the MFILE set. It costs nothing (the harvest already walks that data) and `PROCESS_code_analysis` is waiting on it to write one Owed report covering both loops and both loopholes — the NaN gate and the magnitude-dependent floor — with the filed `equal_nan` report cited. **M100 (per-scenario DSMs) still awaits their user's confirmation**, so `st_regression`'s module boundaries remain extrapolated and the block-arm caveat stands. |
| 2026-09-01 | **M100 (run-all-scenarios) approved by the sibling study's user and executing**, slot-gated behind their gate suite; per-scenario output directories added as their design constraint, both of our riders accepted verbatim. **Deck sha256 published to them ahead of the copy** so their verification is a comparison rather than a self-check — `st_regression` `3f33d565…` (136 043 B), `low_aspect_ratio_DEMO` `72845515…` (30 325 B), both tracked and clean, `st_regression` last changed at `3baf34b8` (the D9 patch). The precaution exists because the deck was untracked until I-9. **V6's exit condition stated explicitly**: if `st_regression`'s own collapsed DSM does not show three modules with internal cycles on the same row ranges, the block-arm result for that scenario is **withdrawn, not caveated**. |
| 2026-09-01 | **I-10: first cross-project discrimination, and the quiet re-baseline deliberately deferred.** The sibling study's content-identical loaded/quiet pair shows CPU-seconds rising ~14 % with the CPU/wall ratio pinned at 1.033/1.035 — identical work consuming more CPU *time*, not queueing — which retires scheduling contention on their workload as it was already retired on ours. Three caveats recorded against over-reading it: CPU time is time rather than cycles, so memory stalls and frequency scaling remain indistinguishable in this metric; `n = 1` under a heterogeneous antagonist; and "load inflates CPU" does not by itself explain our *unloaded* spread. **The agreed decisive test — a quiet re-baseline of ours — is not run, because M100 is executing on the sibling side and the machine is therefore not quiet.** Scheduled for after M100 delivers. **`process-surgery-54` is no longer reachable** (its session ended in a harness restart); its baseline file is recoverable from its transcript if wanted, so the same-tree reconciliation question closes without action. |
| 2026-09-01 | **A20 (registry-append) MERGED** (gates PASS 4/4) **and its code withdrawn in the same merge**, by the user's ruling that synthetic content does not belong in a frozen tree. The three edits — the invented `numerics.framework_placeholder` field, constraint 93 and its `lablcc` label — **stood or fell together**, since constraint 93's body pinned the invented field and the label existed only for the constraint. **The orchestrator mis-framed the decision**, presenting the `lablcc` edit as unconditionally unavoidable under D10 when it is unavoidable only for A4's *real* append; withdrawing the placeholder removes the constraint that needed the label. Corrected in the report and the allocation table. `process/` is now byte-identical to `c0ae5b28` apart from the probe, **F2 allocates nothing, and A4 takes 178/93** — needing no invented field, since `t_plant_pulse_burn` already exists. **What survives, and is the real deliverable:** appending is inert (0 differing MFILE lines, four scenarios); the derived cap grows **twelve** arrays where the table named four; and the input-language divergence is one-way, measured — a fork deck is refused by upstream at *input-parse time* before any model runs, which is exactly the loud failure that reusing one of the 94 gaps would not give, so D10 is now demonstrated rather than argued. **New PROCESS defect found**, verified independently: `set_active_constraints` (`init.py:1280`) bounds its loop by **registry size** rather than the deck's constraint count, so a deck naming more constraints than the registry holds is silently truncated — unreachable in our four decks, same shape as the `equal_nan` defect, recommended for the sibling's bug-report register. **New issue I-11**: the A20 worktree was seeded from upstream `main` with no `arch_surgery/` present; the agent caught it. |
| 2026-09-01 | **A18 (experiment-framework) MERGED — all five gates PASS — and the merge was made before the review was finished, which was the orchestrator's process error.** Protocol **§5 amended** (user): the critical assessment gates the merge; work it turns up goes back to the agent's branch, and the branch merges once. **Three results, two against the plan.** (1) **The two-sweep floor claim is refuted, and it was the orchestrator's own**: published as worth *"up to 31 %"*, measured at **1.53 / 1.55 / 1.79 / 10.7 %**. The bound assumed a sweep saved on every solve; it is saved only where the state is already converged on entry (4.7-30 % of points). Corrected in three documents with the error attributed. The floor is nonetheless **real** — a correct arm converges points in a single sweep, structurally impossible under the current loop. (2) **The unit problem, the most consequential finding.** The block arm **wins on sweeps everywhere and loses by 33-47 % on model evaluations**; both are exact counts. The plan's DSM-row weighting corresponds to neither: **M1 is 24 DSM rows but 2 of the 21 executing model calls**, so §5.3's "M1 dominates the node count" argument is stated in a unit that overstates M1 by an order of magnitude. All three available units are proxies — rows and calls are exact but in the wrong currency, measured cost is the right currency but inexact (I-10) — so **no cost claim may be made without naming its unit**. (3) **The strict predicate is *cheaper* than the incumbent on `low_aspect_ratio_DEMO`** (−3.4 %), a direction the plan did not anticipate. **A0f vindicated**: on `large_tokamak_nof` R→A0 is exactly 0.00 % (9 471 evaluations both ways) decomposing to +1.55 % and −1.53 %, so a study without it would have reported "no effect" from two real opposed effects. **Gates:** neutrality, determinism, harvest inertness, replay fidelity 600/600 and restore exactness all PASS; drop census 0; **no cap ever reached**. Gate F initially failed 599/600 and was root-caused to a real PROCESS property — `call_models` compares a **shorter constraint vector on the `fsolve` path** — then fixed and the whole pipeline regenerated. **τ = 1e-6** by ladder. **Gap found in review and sent back**: the per-quantity scales are computed and never persisted, so the predicate depends on unrecorded data — reproducible but not auditable, and the scales are what separate an excluded quantity from an included one. **New PROCESS finding**: the idempotence loop stops with the cost model still moving on 8/600 design points (`costs.coe`/`coecap`/`coefuelt`), where the fixed-point arms stop with 0/600. |
| 2026-09-01 | **D14 recorded — Phase B is unblocked on approvals.** The `lablcc` edit and the `pulse.py` residual extraction are both approved; Phase B's baseline is **PROCESS as shipped**, not Phase A's flat arm, which makes the comparison single-variable and removes the two-codebases confound that would otherwise have made any timing comparison uninterpretable. The orchestrator's consequent call: module solves reuse PROCESS's own predicate so that architecture is the only thing varying, with both arms carrying `check_agreement`'s known defects deliberately so they cancel. **Still gating Phase B: A22 (outer-pass-census)**, which tests whether `k = 1` holds per pulsed deck — if cross-module movement other than the burn time exists there, lifting will not decouple the modules and the premise fails before F9 or F10 are built. |
| 2026-09-01 | **D15: Phase B is fully specified and unblocked except for A22 (outer-pass-census).** H5 protocol accepted — model evaluations rather than iterations, three-part convergence check including the variant's consistency residual (without which the variant could "win" by returning a point off the manifold), 20-30 paired multi-starts per scenario comparing distributions, **robustness outranking cost**, and outcomes declared in advance including "inconclusive". Design questions settled per D15. **The hoist is inside the variant** at the user's direction, which makes the headline a combined figure and imposes a naming discipline on every report that quotes it. |
| 2026-09-01 | **A22 merged: Phase B's premise holds, and Phase B is unblocked.** `k = 1` is confirmed **per deck, never pooled**. On the three pulsed decks the only cross-module movement is the burn time: holding `times.t_plant_pulse_burn` at its entry value removes **every** above-tau field on outer pass ≥ 2, on 149/149, 297/297 and 10/10 design points, and M2 (Coils) never re-solves at all. `st_regression` has no cross-module movement of any kind — A2 missed nothing. **Assessment before merge (protocol §5):** the orchestrator re-read the four `census.json` artifacts directly and confirmed the gate independently (600/600, `n_compared` equal to the point count on every deck, so the empty-set false-pass of A18's first `gates` predicate cannot recur here), confirmed the burn time is category `continuous` and not `constant` on all three pulsed decks — without which the pin arm would be trivially satisfied — and read the `engine.py` diff to confirm both new hooks are inert at their defaults. **No `process/` change, so no D11 approval was required.** No fixes were owed, so the merge proceeded. **Two things the report did not carry, now recorded:** the finite-difference arithmetic that turns its −29 % into a Phase B expectation (plan §4.1a — about −26 %, and *conditional on the optimiser's major-iteration count*, which nothing yet bounds); and **I-12**, the diverging-cost-model mechanism that inflated 7 of 144 outer-pass counts and which D15's perturbed multi-starts will meet again by design. |
| 2026-09-01 | **A3 merged: the sequence is now a driver choice, and the reorder costs nothing.** `build` moves from inside M1's span to the head of M2's, making M1 contiguous — the precondition for a per-module solver, so A5/F10 are unblocked. Gate bit-identical 4/4 with stated denominators, `ifail` unchanged, per-node call counts equal node by node. **Assessment before merge (protocol §5):** the orchestrator read the `caller.py` diff and confirmed the default path is the upstream order with identical semantics, that an unrecognised `PROCESS_ARCH_SEQUENCE` raises rather than silently defaulting, and that nothing under `process/models/` is touched. No fixes were owed. **The review found something the agent could not have:** cross-reading A3 against A18's `arms.py` shows `build_blocks` groups by module and so *already* ran A3's order, while `loop_nodes` keeps the upstream order — so **Phase A's A0 → A1 varied the grouping and this permutation together**, an unnamed confound in a headline comparison. A3 retires most of it (the permutation is bit- and sweep-count-identical in R); **A23 (flat-arm-permutation)** is queued to close the rest by replay, before A21 words the result. **Two practices promoted to protocol** from what A3 did unprompted: §12, a gate must be shown capable of failing and every mismatch count carries its denominator; §13, the branch-point check is mandatory in every brief, because **I-11 has recurred and the harness seeding from `main` must now be assumed**. |
| 2026-09-01 | **A13 merged: the hoist is real, costs nothing, and is worth 6.56 / 6.76 / 6.64 / 2.63 % of model evaluations.** Sweeps are unchanged — it removes work *within* sweeps. Built as VP2/F7b on A3's VP1 pattern, default off, node set derived at run time so `pulse` joins once A4 lifts the coupler. **Assessment before merge (protocol §5):** the orchestrator read the `caller.py` diff and checked the one path the report did not discuss — what happens when the idempotence loop exhausts its 10 iterations without converging. It **raises**, so the deferred tail never silently goes unrun and there is no stale-output path; that concern is closed rather than merely unraised. Also confirmed: the hoist-off branch creates no deferral list at all, the final-output path explicitly disables deferral, and `abs()` is applied to the figure of merit so a maximise (negative) setting resolves correctly. No fixes owed. **One claim narrowed:** §6's "three decks agree to three significant figures" is an **algebraic identity, not a confirmation** — restating A2's cost model on A13's node set recomputes the saving from this run's own sweep and call counts, so it cannot disagree. What §6 does establish, and it is worth having, is that the apparent 4.6–8.2 % vs 6.6 % gap is **entirely** node set and unit, with no unexplained residue; plan §4.1 is corrected accordingly. **The finding that outranks the saving:** the hoistable node set depends on the active **figure of merit**, because `objectives.py` reads `costs.coe` (6) and `costs.cdirt`/`concost` (7) — so the tail is not a fixed property of the code but of the deck. **I-13** records the consequence: Phase A's `hoist = 1` and Phase B's hook disagree on `large_tokamak_eval`, and A21 owes the reader that sentence. **I-11 has a third instance**, and protocol §13 is sharpened — upstream `main` descends from `c0ae5b28`, so the descent check alone passes on exactly the tree it is meant to catch; only the named-tip half works. |
| 2026-09-01 | **A23 merged: Phase A's headline comparison is clean.** Arm A0 in the block arm's node order reproduces A18's recorded A0 **bit-for-bit on 600/600 design points**, and on **2 400/2 400** across every setting A18 recorded it under, so **`A0 → A1` is the module grouping alone** and the confound found during A3's review is retired. **Assessment before merge (protocol §5):** the orchestrator re-derived the gate from the four `result.json` artifacts rather than the report — `G2` identical with zero skips on every deck, `replay_never_entered_caller` true on all four, sensitivity arms moving 488/600 and 575/600. Nothing was narrowed; the agent had already caught and removed its own T11-shaped error (a draft explanation of the 112 ULP-inert points that its own sweep-count distribution contradicted). **The methodological result may outlast the null:** A22's tree-hash licence for reusing A18's harvest **stopped working** once A3 and A13 touched `caller.py`, and the replacement is better — the sub-trees that determine model behaviour are hash-identical to each harvest commit, and the driver is entered **0 times** during a replay, counted. That last point cuts both ways and A23 said so: **A3's and A13's bit-identity gates do not license this reuse at all**, because they gate a path the replay never executes. Any future task reusing the harvest should make this argument, not the tree-hash one. **Recorded as V9a** in the DSM register: the DSM interleaves `build`'s row 5 inside `physics`'s rows 4 and 6–28, yet transposing the two call sites is inert at the trajectory level — **row adjacency is not call-level coupling**, a third instance of DSM granularity misleading a reader. **I-11 now has four instances — every task since the check was written.** |
| 2026-09-01 | **I-11 resolved by restructuring dispatch, and the run artifacts consolidated** (user: "do 2"). Root cause measured, not guessed: the harness seeds worktrees from the **default branch**, `main` is `6df46205`, and **7 of 7** worktrees ever created here landed there — so the issue was never intermittent and A18 was not the exception previously recorded. Worse, `main` carries no `CLAUDE.md`, no `arch_surgery/` and no `TRAPS.md`, so a mis-seeded agent has **no** project rules loaded; the guard cannot live in the tree it guards. **The orchestrator now creates task worktrees itself** with `arch_surgery/bin/new_task_worktree.sh` — asserts named tip, descent from the frozen base, not-upstream-`main`, and the presence of `arch_surgery/`/`CLAUDE.md`/`TRAPS.md`; tears itself down on any failure so a wrong tree is neither handed over nor left behind — and dispatches **without** harness isolation. **Housekeeping done in the same pass:** all seven stale worktrees removed and every merged task branch deleted, with **~1.4 GB of run artifacts relocated rather than discarded** into `arch_surgery/idf_probe/runs/` (untracked) under per-task names `a3/ a13/ a18/ a20/ a22/ a23*/`; A20's and A3's were unnamespaced and would have collided with the A1-era directories already there, so they were given their own. **This has a second benefit that was not the motivation:** `run_phase_a.py`, `run_a22.py`, `run_a23.py` and `analyse.py` all resolve `runs/<task>` in the main tree, so **A18's harvest is now reachable without reaching into a locked worktree** — which is how A22 and A23 had to get at it, and which would have broken the moment those worktrees were cleaned up. |
| 2026-09-01 | **Phase A write-up and Phase B implementation both dispatched, through the new worktree path.** A21 (partition-report) writes Phase A up from the recorded artifacts as **light** work — explicitly forbidden from re-running the experiment, since one heavy slot is held by A24 — and carries six named reporting constraints, each one something this project has already been burned by: the hoist's corrected figure, A23's narrow licence, `k` not transferring between decks, I-12, I-13, and no conclusion resting on a timing. It is also told to surface or explicitly flag as owed the `objf`/constraint magnitude distribution promised to `PROCESS_code_analysis`. A24 (phase-b-scaffold) holds the heavy slot. **Both worktrees were created by `new_task_worktree.sh` and both passed its six assertions** — the first dispatches since I-11 was resolved, and the first that did not need the agent to check its own base. |
| 2026-09-01 | **A24 and A21 merged: Phase B's scaffolding is in and Phase A is written up.** A24's bundle gate passes at **0 of 121 295** quantities on all four decks, so **D16's bundling premise held** — all three inert pieces shared one gate and it was sharp. The one model edit in the whole experiment, `pulse.py`, was read line by line before merge: the arithmetic moves verbatim into `burn_time_root`, the negative-burn-time diagnostic is preserved, and `subsolve`'s default is a pure passthrough to `direct(*args)`. **A24's premise correction is now plan §4.1b:** the burn-time site is closed-form, not an inner root-find, so the lift buys **loop topology, not inner-solve work**, and A25 must not claim otherwise. **A21 corrected three statements in A18's merged report** — the hoist figure being loop-only and omitting `hoist_tail_node_calls` (net: 6.38 / 6.55 / 13.70 / 5.71 %); the outer-count claim being measured against R rather than the flat control (36 and 100 against A0, and 'rises on none' failing against R on two decks); and an exit-residual value for A1. All three were re-derived independently by the orchestrator from the artifacts before merge and are recorded as a correction note on A18 itself, so a reader of that report meets them first. **The `objf`/constraint magnitude distribution owed to `PROCESS_code_analysis` is discharged**, in results §4.6. **Third task running: the §12 sensitivity check again caught a defect in the agent's own harness.** A3, A13 and now A24 — in every case the gate was passing before the check was run. That is the rule earning its place, not a run of bad luck. |
| 2026-09-01 | **Phase B is complete, and the result is negative: the proposed architecture does not win.** The equivalence gate passed 4/4, H5 ran to the protocol declared in advance, and the variant loses or ties on three decks and is inconclusive on the fourth — never solving a start the baseline cannot. **This is the experiment working, not the experiment failing.** **Assessment before merge (protocol §5) overturned the report's own mitigating caveat.** A25 argued its 13 refused starts were an artifact of `check_agreement(nan, nan)` returning `True`, which would mean the baseline was credited with NaN states. The loophole is real — the orchestrator confirmed it directly — but **it did not operate here**: on all 13 starts the baseline's final MFILE is entirely finite, `0` of 13, with a detector shown capable of firing first (it flags 9 of 300 MFILEs). The real mechanism is that **the per-module solve examines intermediate module state the global loop never tests**, so a transient non-finite value inside M1 is fatal to the variant under D15(d) while the baseline passes through it. That makes the robustness deficit **genuine** and the negative verdict **stronger** than reported. The agent was right not to re-run with the field excluded — that would have been tuning; the error was inferring from the loophole's existence to its having operated. **Open, and worth proposing as a task:** whether D15(d)'s raise-on-transient *policy*, rather than the architecture, costs those starts. That is a decision, not an architectural necessity, and it must be measured rather than assumed. |
| 2026-09-02 | **A26 merged, and Phase A's headline is overturned by this project's own critical pass.** The partition does not cost 47 %; at matched achieved accuracy it costs slightly less than the flat arrangement on all three retained decks. **Assessment before merge (protocol §5):** the orchestrator re-derived the envelope from `matched_accuracy.json` rather than the report. The construction `cost(a) = min{cost_i : accuracy_i ≤ a}` is the correct Pareto reading of "what does this arm cost to reach accuracy *a*", and reading rungs in tolerance order — which gave the agent +21.9 % — is the wrong one. The comparison at the tightest matched accuracy is exact rather than interpolated: block `inner0.1` achieves `1.2556721063507803e-11`, **bit-identical** to flat `tau1e-06`, for 9 062 model evaluations against 9 471, at 2.792 mean sweeps against 3.027. **The mechanism matters more than the number:** at that setting the block arm has largely stopped blocking, so the finding is that **over-solving was the cost**, not that blocking wins. The standing results document is bannered accordingly and A28 rewrites it. **Verified independently:** three constants moved, not the one §6.3 claims — `ccfe_hcpb.x_shield` on `st_regression` plus two `physics` fields on `large_tokamak_eval`, 63 arm-records each. **The scale-floor change turned out free** (0 of 590 points invalidated), so my earlier warning that Phase A's counts would move from that quarter was wrong — they move from the accuracy matching instead. |
| 2026-09-02 | **A28 merged and the follow-on cycle dispatched; A27's handoff is delivered, filed, and closed both ways.** A28 assessed against raw per-start metrics before merge (`0c18dfcc`): the Phase B verdict moves to **wins one deck / inconclusive one / turns on a single start on the third**, with two thirds of the refusal deficit attributed to the **predicate**, not the architecture — the measurement D18's third arm existed for. The matched-count envelope **flips Phase A's sign on two decks** (+33.4/+27.4/−15.2 % against all-settings −4.3/−4.5/−13.1); both constructions now published, the honest object the bracket. Worktree retired via the script, all 617 run records relocated — **the regenerated `runs/a26/matched_accuracy.json` restores what I-14 destroyed**. A29 (replication-verify, heavy slot) and A30 (phase-b-critique, light) dispatched off the merged tip through `new_task_worktree.sh`. **The PROCESS-defect handoff is confirmed filed by the `PROCESS_code_analysis` orchestrator** (their commit `0a6330a`): report copied verbatim to their `docs/bug_reports/`, the 2026-09-01 report kept with an amendment banner per the supersession note, and our §B measurement **discharged a debt on their books** (the `atol=1e-8` constraint-vector population their README carried as Owed). Nothing further owed in either direction on that handoff; they may later request the multi-start artifacts behind the Phase B addendum for their M20 consolidation. |
